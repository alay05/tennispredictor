---
phase: 01-foundation-and-atp-data-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .python-version
  - .gitignore
  - .env.example
  - src/tennisprediction/__init__.py
  - src/tennisprediction/config.py
  - src/tennisprediction/logging.py
  - src/tennisprediction/cli.py
  - tests/unit/test_cli_smoke.py
autonomous: false
requirements:
  - FND-01
must_haves:
  truths:
    - "Developer can bootstrap the Phase 1 Python environment from tracked project metadata after approving the dependency gate."
    - "Developer can run linting, formatting, typing, and test commands from the repository root."
    - "Typed settings and logging entrypoints exist before any Phase 1 source-acquisition code is added."
  artifacts:
    - path: "pyproject.toml"
      provides: "Pinned Python project metadata, dependency groups, and tool configuration"
      contains: "[project]"
    - path: "src/tennisprediction/config.py"
      provides: "Typed settings loader for project paths and runtime options"
      exports: ["Settings", "get_settings"]
    - path: "src/tennisprediction/cli.py"
      provides: "Minimal CLI bootstrap for downstream Phase 1 commands"
      exports: ["app", "main"]
    - path: "tests/unit/test_cli_smoke.py"
      provides: "Smoke coverage for config/logging/CLI bootstrap"
      contains: "test_cli"
  key_links:
    - from: "pyproject.toml"
      to: "src/tennisprediction/cli.py"
      via: "console script entrypoint"
      pattern: "tennisprediction"
    - from: "src/tennisprediction/cli.py"
      to: "src/tennisprediction/config.py"
      via: "settings bootstrap"
      pattern: "get_settings"
    - from: "src/tennisprediction/logging.py"
      to: "src/tennisprediction/cli.py"
      via: "logging initialization"
      pattern: "configure_logging"
---

<objective>
Create the Phase 1 Python project skeleton and quality gates that every later data-foundation plan builds on, while keeping Phase 1 strictly inside the ingestion/validation/normalization boundary per D-10 and D-11.

Purpose: Establish the reproducible runtime, typed settings, logging, and verification commands before any Sackmann source code is added.
Output: A `uv`-managed Python package, minimal CLI/bootstrap modules, and automated lint/type/test commands after a blocking package-legitimacy checkpoint.
</objective>

<execution_context>
@/Users/andrewlay/.codex/gsd-core/workflows/execute-plan.md
@/Users/andrewlay/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-atp-data-contracts/01-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@project-outline.yaml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve the Phase 1 dependency list before any package-manager install</name>
  <files>pyproject.toml, uv.lock</files>
  <action>Review and approve the exact Phase 1 runtime/dev dependency list before the executor runs `uv lock` or `uv sync`. Because the research set does not contain the required package-legitimacy audit table, keep installation blocked until the human gate explicitly approves or edits the package list.</action>
  <what-built>Phase 1 uses Python packages from the research stack, but the research artifacts do not include the required `## Package Legitimacy Audit` table. Treat every package in the initial Phase 1 runtime/dev set as `[ASSUMED]` until the human gate approves it.</what-built>
  <verify>
    <automated>MISSING — human package-legitimacy review must complete before `uv lock` or `uv sync` is allowed.</automated>
    <human-check>
      1. Review the proposed Phase 1 packages on PyPI: `polars`, `pyarrow`, `duckdb`, `pydantic`, `pydantic-settings`, `typer`, `rich`, `pytest`, `ruff`, `mypy`, `pre-commit`.
      2. Confirm each package is acceptable for this repo or replace it before installation.
      3. Resume only after explicitly approving the list.
    </human-check>
  </verify>
  <done>Package legitimacy is approved or amended, and the executor has an explicit green light to generate `uv.lock` and install only the approved Phase 1 packages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the `uv` project skeleton and typed runtime bootstrap</name>
  <files>pyproject.toml, .python-version, .env.example, src/tennisprediction/__init__.py, src/tennisprediction/config.py, src/tennisprediction/logging.py</files>
  <behavior>
    - Test 1: `Settings` loads required project paths and runtime options from environment/defaults without reaching outside the repo.
    - Test 2: logging bootstrap exposes one project logging entrypoint that downstream commands can call consistently.
  </behavior>
  <action>Create a PEP 621 `pyproject.toml` pinned to Python 3.12 and `uv`, with only the Phase 1 runtime/dev dependency groups needed for data foundation work; do not pre-install modeling or Kalshi packages because D-10 keeps Phase 1 scoped to ingestion/validation/normalization. Add Ruff, mypy, and pytest config in `pyproject.toml`, a `.python-version`, and `.env.example` describing repo-local paths only. Implement typed settings in `src/tennisprediction/config.py` and a single logging bootstrap in `src/tennisprediction/logging.py` so later plans reuse the same path/config conventions.</action>
  <verify>
    <automated>uv sync --group dev && uv run python -c "from tennisprediction.config import get_settings; from tennisprediction.logging import configure_logging; get_settings(); configure_logging()"</automated>
  </verify>
  <done>`pyproject.toml` defines the Phase 1 package/runtime/tooling contract, and the typed settings/logging modules load successfully through `uv run`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add CLI/bootstrap smoke coverage and lock the environment</name>
  <files>uv.lock, .gitignore, src/tennisprediction/cli.py, tests/unit/test_cli_smoke.py</files>
  <behavior>
    - Test 1: the CLI entrypoint boots config + logging and exits successfully for a smoke command such as `version` or `health`.
    - Test 2: linting, formatting, typing, and pytest run from the repo root after `uv sync --group dev`.
  </behavior>
  <action>Add a minimal Typer CLI in `src/tennisprediction/cli.py` that initializes settings and logging but does not introduce any walking-skeleton or end-to-end data slice per D-11. Add a smoke test in `tests/unit/test_cli_smoke.py`, a `.gitignore` suited to `uv`, Python caches, and local data artifacts, then generate `uv.lock` only after the dependency checkpoint is approved so Phase 1 has a tracked, reproducible environment artifact.</action>
  <verify>
    <automated>uv sync --group dev && uv run pytest tests/unit/test_cli_smoke.py -q && uv run ruff check . && uv run ruff format --check . && uv run mypy src</automated>
  </verify>
  <done>The repo has a locked Phase 1 environment, a minimal bootstrap CLI, and one repeatable command suite for test/lint/format/type checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer workstation→package resolver | Unreviewed third-party packages could enter the environment before legitimacy is checked |
| environment variables→typed settings | Misconfigured or secret-bearing env values cross into application config here |
| CLI bootstrap→logging/config modules | Early runtime failures can hide configuration mistakes that later data jobs would inherit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | `pyproject.toml` dependency set | mitigate | Add the blocking package-legitimacy checkpoint before any `uv lock`/`uv sync`; install only the approved Phase 1 packages. |
| T-01-01-02 | Information Disclosure | `.env.example`, `config.py` | mitigate | Keep only non-secret placeholders in `.env.example`; load secrets from environment at runtime and never hardcode credentials or private paths. |
| T-01-01-03 | Repudiation | `uv.lock`, CLI bootstrap | mitigate | Track `uv.lock`, standardize `uv run` commands, and keep one bootstrap path so later plans have an auditable runtime contract. |
| T-01-01-SC | Tampering | Python package installs | mitigate | RESEARCH lacks a package legitimacy audit table, so the install path stays human-gated until the dependency list is approved. |
</threat_model>

<verification>
- `uv sync --group dev`
- `uv run pytest tests/unit/test_cli_smoke.py -q`
- `uv run ruff check .`
- `uv run ruff format --check .`
- `uv run mypy src`
</verification>

<success_criteria>
- Phase 1 has a tracked Python 3.12/`uv` project skeleton with typed settings and logging.
- A minimal CLI boots successfully without adding any feature/model/Kalshi behavior.
- Linting, formatting, typing, and pytest all run from the repo root after the dependency gate is approved.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-and-atp-data-contracts/01-01-SUMMARY.md` when done
</output>
