---
phase: 01-foundation-and-atp-data-contracts
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/tennisprediction/domain/__init__.py
  - src/tennisprediction/domain/ids.py
  - src/tennisprediction/domain/models.py
  - src/tennisprediction/domain/normalization.py
  - src/tennisprediction/storage/duckdb.py
  - docs/data-contracts.md
  - tests/unit/test_normalization.py
autonomous: true
requirements:
  - FND-04
  - FND-06
must_haves:
  truths:
    - "Canonical ATP-only player, tournament, match, ranking, and match-stat tables exist with stable IDs and lineage."
    - "Canonical outputs exclude quarantined qualifiers, retirements, walkovers, incomplete matches, and out-of-scope inputs."
    - "Developer can inspect one Phase 1 document that explains missing stats, qualifiers, retirements, walkovers, incomplete matches, ATP-only scope, and ID/lineage rules."
  artifacts:
    - path: "src/tennisprediction/domain/ids.py"
      provides: "Stable source-derived and synthetic canonical ID rules"
      exports: ["CanonicalIdFactory"]
    - path: "src/tennisprediction/domain/models.py"
      provides: "Canonical table contracts for players, tournaments, matches, rankings, and match stats"
      exports: ["CanonicalPlayer", "CanonicalTournament", "CanonicalMatch", "CanonicalRanking", "CanonicalMatchStat"]
    - path: "src/tennisprediction/domain/normalization.py"
      provides: "Validated-to-canonical normalization pipeline"
      exports: ["normalize_snapshot"]
    - path: "docs/data-contracts.md"
      provides: "Phase 1 data rules and scope boundary documentation"
      contains: "qualifiers"
  key_links:
    - from: "src/tennisprediction/domain/normalization.py"
      to: "src/tennisprediction/ingestion/validation.py"
      via: "validated snapshot input"
      pattern: "validate"
    - from: "src/tennisprediction/domain/normalization.py"
      to: "src/tennisprediction/domain/ids.py"
      via: "ID assignment"
      pattern: "CanonicalIdFactory"
    - from: "src/tennisprediction/storage/duckdb.py"
      to: "src/tennisprediction/domain/models.py"
      via: "canonical table persistence"
      pattern: "CanonicalMatch"
---

<objective>
Create the canonical Phase 1 domain tables and the documented v1 data rules per D-06 through D-10, using only validated inputs from Plan 01-03.

Purpose: Lock the canonical contracts that later feature/modeling phases will consume without reopening Phase 1 identity or scope decisions.
Output: Canonical domain models, deterministic ID rules, validated-to-canonical normalization, DuckDB persistence helpers, and written data-handling rules.
</objective>

<execution_context>
@/Users/andrewlay/.codex/gsd-core/workflows/execute-plan.md
@/Users/andrewlay/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-atp-data-contracts/01-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@project-outline.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define canonical domain contracts and deterministic ID rules</name>
  <files>src/tennisprediction/domain/__init__.py, src/tennisprediction/domain/ids.py, src/tennisprediction/domain/models.py, tests/unit/test_normalization.py</files>
  <behavior>
    - Test 1: source-derived IDs are reused where Sackmann provides them, and synthetic canonical IDs are generated only for entities that lack stable upstream IDs.
    - Test 2: canonical records preserve lineage fields pointing back to the raw manifest and source row identity.
  </behavior>
  <action>Define canonical contracts for player, tournament, match, ranking, and match-stat tables with source lineage fields per D-06, D-07, and D-08. Implement deterministic ID helpers that reuse Sackmann player or other source IDs where available, mint synthetic IDs only for matches/tournaments/derived records that need them, and explicitly avoid manual player-merge logic per D-09.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_normalization.py -q</automated>
  </verify>
  <done>Canonical table models and ID rules exist, and tests prove lineage preservation plus deterministic synthetic ID generation where needed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize validated data into canonical tables and document Phase 1 rules</name>
  <files>src/tennisprediction/domain/normalization.py, src/tennisprediction/storage/duckdb.py, docs/data-contracts.md, tests/unit/test_normalization.py</files>
  <behavior>
    - Test 1: normalization accepts only validated, in-scope candidates and excludes quarantined rows from canonical tables.
    - Test 2: the documented rules match the implemented handling for missing stats, qualifiers, retirements, walkovers, incomplete matches, ATP-only scope, and lineage/ID policy.
  </behavior>
  <action>Implement `normalize_snapshot` so canonical tables are built only from validated, non-quarantined inputs per D-03, D-04, and D-05. Add DuckDB persistence helpers that write canonical players, tournaments, matches, rankings, and match stats as Phase 1 tables without mixing in feature, model, backtest, or Kalshi concerns per D-10. Write `docs/data-contracts.md` that documents the exact Phase 1 rules for missing stats, qualifiers, retirements, walkovers, incomplete matches, ATP-only scope boundaries, source-derived IDs, synthetic IDs, and lineage fields, and explicitly note that no walking skeleton or end-to-end feature slice belongs in Phase 1 per D-11.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_normalization.py -q</automated>
  </verify>
  <done>Validated inputs normalize into canonical ATP-only tables with deterministic IDs and lineage, and the written Phase 1 rules match the implemented exclusion/handling behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validated candidate rows→canonical tables | Only approved, in-scope data should cross into canonical persistence |
| canonical models→DuckDB tables | Contract drift here can corrupt downstream feature/model inputs |
| implementation→documentation | The written rules must accurately reflect the real exclusion and ID behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Tampering | `normalization.py` | mitigate | Consume only validated, non-quarantined inputs and assert canonical exclusion rules in tests. |
| T-01-04-02 | Spoofing | `ids.py` | mitigate | Reuse source IDs where present and make synthetic IDs deterministic from source fields so the same entity cannot drift across runs. |
| T-01-04-03 | Repudiation | canonical tables/docs | mitigate | Preserve raw manifest/source lineage on canonical records and document each exclusion/handling rule in `docs/data-contracts.md`. |
| T-01-04-04 | Information Disclosure | `duckdb.py`, docs | accept | Phase 1 stores public tennis data and repo-local paths only; no secrets should be persisted in DuckDB tables or docs. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_normalization.py -q`
</verification>

<success_criteria>
- Canonical player, tournament, match, ranking, and match-stat contracts exist with stable IDs and source lineage.
- Canonical outputs exclude quarantined or out-of-scope data by construction.
- Phase 1 has one written data-rules document that matches the implementation for missing stats and excluded match types.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-and-atp-data-contracts/01-04-SUMMARY.md` when done
</output>
