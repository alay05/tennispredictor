---
phase: 05-kalshi-read-only-market-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/tennisprediction/kalshi/jobs.py
  - src/tennisprediction/cli.py
  - src/tennisprediction/kalshi/retry.py
  - tests/unit/test_kalshi_jobs.py
autonomous: true
requirements:
  - KAL-03
  - KAL-05
must_haves:
  truths:
    - "System handles Kalshi pagination, retries, rate-limit/backoff behavior, and closed or settled market states."
    - "System can run a read-only snapshot collection job without placing or preparing orders."
    - "The collector is deterministic, read-only, and resilient to transient 429s and pagination boundaries."
  artifacts:
    - path: "src/tennisprediction/kalshi/retry.py"
      provides: "Bounded retry/backoff helpers for read-only Kalshi requests"
      exports: ["retry_kalshi_read_call"]
    - path: "src/tennisprediction/kalshi/jobs.py"
      provides: "Snapshot collection job and pagination/state-handling orchestration"
      exports: ["collect_kalshi_snapshots"]
    - path: "tests/unit/test_kalshi_jobs.py"
      provides: "Regression coverage for pagination, 429 backoff, state handling, and read-only guardrails"
  key_links:
    - from: "src/tennisprediction/kalshi/jobs.py"
      to: "src/tennisprediction/kalshi/client.py"
      via: "read endpoint orchestration"
      pattern: "list_markets|get_market|get_market_orderbook"
    - from: "src/tennisprediction/kalshi/jobs.py"
      to: "src/tennisprediction/kalshi/storage.py"
      via: "snapshot persistence of paged results"
      pattern: "persist_kalshi_snapshot_batch"
    - from: "src/tennisprediction/kalshi/jobs.py"
      to: "https://docs.kalshi.com/getting_started/rate_limits"
      via: "token-bucket and 429 retry behavior"
      pattern: "429|backoff"
---

<objective>
Create the deterministic read-only snapshot collection workflow.
Output: cursor-based pagination, bounded retries, market-state handling, and a job/CLI surface that can collect snapshots without any execution path.
</objective>

## Phase Goal

**As a** developer, **I want to** collect Kalshi market snapshots safely and repeatably, **so that** later phases can map and score market opportunities from persisted data instead of live transport state.

<execution_context>
@/Users/andrewlay/.codex/gsd-core/workflows/execute-plan.md
@/Users/andrewlay/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-kalshi-read-only-market-integration/05-RESEARCH.md
@.planning/phases/05-kalshi-read-only-market-integration/05-PATTERNS.md
@src/tennisprediction/cli.py
@src/tennisprediction/config.py
@src/tennisprediction/storage/duckdb.py
</context>

## Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Pagination, retries, state handling, and read-only collection job | 05-03 | COVERED | Final phase-5 slice. |
| REQ | KAL-03 | Pagination, retries, rate limits, and market states | 05-03 | COVERED | Retry and state logic live here. |
| REQ | KAL-05 | Read-only snapshot collection job | 05-03 | COVERED | Job and CLI surface are read-only. |
| RESEARCH | R-01 | `Get Markets` pagination and status filters | 05-03 | COVERED | Cursor-driven loop. |
| RESEARCH | R-02 | 429 handling and token-bucket rate limits | 05-03 | COVERED | Bounded backoff. |
| CONTEXT | — | ATP-only, Kalshi-only, repo-local paths, no write surfaces | 05-03 | COVERED | Hard boundary. |

## Artifacts this plan produces

- Files: `src/tennisprediction/kalshi/retry.py`, `src/tennisprediction/kalshi/jobs.py`, `src/tennisprediction/cli.py`, `tests/unit/test_kalshi_jobs.py`
- Functions: `retry_kalshi_read_call`, `collect_kalshi_snapshots`
- CLI surface: one read-only snapshot collection command

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for pagination, retry/backoff, market-state handling, and read-only guardrails</name>
  <read_first>
https://docs.kalshi.com/api-reference/market/get-markets
https://docs.kalshi.com/getting_started/rate_limits
tests/unit/test_kalshi_client.py
tests/unit/test_kalshi_storage.py
  </read_first>
  <files>tests/unit/test_kalshi_jobs.py</files>
  <behavior>
    - Test 1: paginated market listing walks cursors deterministically and stops on exhaustion.
    - Test 2: 429 responses trigger bounded retry/backoff behavior for read-only endpoints.
    - Test 3: closed, settled, paused, and unopened market states are recorded and handled explicitly.
    - Test 4: the collector refuses any code path that would imply order placement or execution prep.
  </behavior>
  <action>Create the tests before the job implementation so the collection workflow cannot accidentally grow write-side behavior or unbounded retry loops.</action>
  <verify>
    <automated>! python3 -m uv run pytest -q tests/unit/test_kalshi_jobs.py -x</automated>
  </verify>
  <done>The job tests exist and fail before the collection workflow is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the read-only snapshot collection job and CLI entrypoint</name>
  <read_first>
src/tennisprediction/kalshi/client.py
src/tennisprediction/kalshi/storage.py
tests/unit/test_kalshi_jobs.py
https://docs.kalshi.com/api-reference/market/get-markets
https://docs.kalshi.com/api-reference/historical/get-historical-markets
  </read_first>
  <files>src/tennisprediction/kalshi/retry.py, src/tennisprediction/kalshi/jobs.py, src/tennisprediction/cli.py, tests/unit/test_kalshi_jobs.py</files>
  <action>Implement pagination, bounded retries, market-state handling, and a read-only collection job that writes snapshots through the persistence layer. Add the smallest possible CLI entrypoint for running the collector, and keep the command surface read-only by construction.</action>
  <verify>
    <automated>python3 -m uv run pytest -q tests/unit/test_kalshi_jobs.py -x</automated>
  </verify>
  <done>Phase 05 has a deterministic read-only snapshot collection workflow with explicit retry and state handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network→job | Retry/backoff must be bounded and predictable under 429 and transient failures |
| job→storage | The job must only write snapshot rows, never execution artifacts |
| job→CLI | The CLI must expose only read-only collection behavior in Phase 05 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Denial of Service | pagination/retry loop | mitigate | Bound retries, detect exhausted cursors, and stop on terminal pages. |
| T-05-03-02 | Elevation of Privilege | job orchestration | mitigate | Keep the collector read-only and refuse any execution-prep or order-related call path. |
| T-05-03-03 | Repudiation | market-state handling | mitigate | Persist state and cursor metadata so every collected snapshot can be traced to its request context. |
</threat_model>

<verification>
- `! python3 -m uv run pytest -q tests/unit/test_kalshi_jobs.py -x`
- `python3 -m uv run pytest -q tests/unit/test_kalshi_client.py tests/unit/test_kalshi_storage.py tests/unit/test_kalshi_jobs.py -x`
- `python3 -m uv run ruff check src/tennisprediction/kalshi tests/unit/test_kalshi_jobs.py`
- `python3 -m uv run mypy src/tennisprediction/kalshi`
</verification>

<success_criteria>
- Developer can run a read-only snapshot collection workflow with cursor-based pagination.
- 429s are handled with bounded retry/backoff.
- No order-placement or execution-prep path exists in Phase 05.
</success_criteria>

<output>
Create `.planning/phases/05-kalshi-read-only-market-integration/05-03-SUMMARY.md` when done
</output>

