---
phase: 05-kalshi-read-only-market-integration
plan: 01
type: execute
wave: 1
depends_on:
  - 04-03
files_modified:
  - src/tennisprediction/kalshi/__init__.py
  - src/tennisprediction/kalshi/schemas.py
  - src/tennisprediction/kalshi/client.py
  - tests/unit/test_kalshi_client.py
autonomous: true
requirements:
  - KAL-01
  - KAL-04
must_haves:
  truths:
    - "Developer can configure authenticated Kalshi read access for market listing, market detail, and orderbook retrieval."
    - "Business logic consumes normalized project DTOs instead of raw Kalshi payloads."
    - "The client boundary is read-only and does not expose order-placement primitives."
  artifacts:
    - path: "src/tennisprediction/kalshi/client.py"
      provides: "Authenticated read client with signing, request helpers, and read endpoint methods"
      exports: ["KalshiReadClient", "KalshiRequestError"]
    - path: "src/tennisprediction/kalshi/schemas.py"
      provides: "Frozen DTO contracts for markets, market detail, orderbooks, and request metadata"
      exports: ["KalshiMarketDTO", "KalshiMarketDetailDTO", "KalshiOrderbookDTO", "KalshiRequestMetadata"]
    - path: "tests/unit/test_kalshi_client.py"
      provides: "Regression coverage for request signing, endpoint selection, and DTO normalization"
  key_links:
    - from: "src/tennisprediction/kalshi/client.py"
      to: "src/tennisprediction/config.py"
      via: "repo-local environment and endpoint configuration"
      pattern: "BaseSettings"
    - from: "src/tennisprediction/kalshi/client.py"
      to: "https://docs.kalshi.com/getting_started/api_keys"
      via: "RSA-PSS signed read requests"
      pattern: "KALSHI-ACCESS-KEY|KALSHI-ACCESS-TIMESTAMP|KALSHI-ACCESS-SIGNATURE"
    - from: "src/tennisprediction/kalshi/client.py"
      to: "https://docs.kalshi.com/api-reference/market/get-markets"
      via: "paged market listing and filters"
      pattern: "cursor|limit|status"
---

<objective>
Create the project-owned Kalshi read client and DTO boundary for markets, market detail, and orderbooks.
Output: authenticated read methods, typed normalized responses, and failing-then-passing tests that prove the client is read-only and transport details stay out of business logic.
</objective>

## Phase Goal

**As a** developer, **I want to** read Kalshi market data through project-owned interfaces, **so that** later snapshot, mapping, and EV code does not depend on raw SDK payloads.

<execution_context>
@/Users/andrewlay/.codex/gsd-core/workflows/execute-plan.md
@/Users/andrewlay/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-kalshi-read-only-market-integration/05-RESEARCH.md
@.planning/phases/05-kalshi-read-only-market-integration/05-PATTERNS.md
@src/tennisprediction/config.py
@src/tennisprediction/modeling/schemas.py
@src/tennisprediction/storage/duckdb.py
</context>

## Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Authenticated read access with normalized DTOs and no write surfaces | 05-01 | COVERED | Establishes the transport/domain boundary. |
| REQ | KAL-01 | Read client with market listing, detail, and orderbook retrieval | 05-01 | COVERED | Read methods and signing. |
| REQ | KAL-04 | Keep SDK/API payloads behind project-owned interfaces | 05-01 | COVERED | DTO layer is part of the slice. |
| RESEARCH | R-01 | Kalshi auth requires signed request headers | 05-01 | COVERED | Signing tests lock the boundary. |
| RESEARCH | R-02 | SDKs may lag; OpenAPI is source of truth | 05-01 | COVERED | Direct transport adapter, not business-logic coupling. |
| CONTEXT | — | ATP-only, Kalshi-only, repo-local config and paths | 05-01 | COVERED | Derived from `AGENTS.md`, `PROJECT.md`, and `STATE.md`. |

## Artifacts this plan produces

- Files: `src/tennisprediction/kalshi/__init__.py`, `src/tennisprediction/kalshi/schemas.py`, `src/tennisprediction/kalshi/client.py`, `tests/unit/test_kalshi_client.py`
- DTOs: `KalshiMarketDTO`, `KalshiMarketDetailDTO`, `KalshiOrderbookDTO`, `KalshiRequestMetadata`
- Functions/classes: `KalshiReadClient`, `KalshiRequestError`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for authenticated read access and DTO normalization</name>
  <read_first>
src/tennisprediction/config.py
src/tennisprediction/modeling/schemas.py
https://docs.kalshi.com/getting_started/api_keys
https://docs.kalshi.com/api-reference/market/get-markets
https://docs.kalshi.com/api-reference/market/get-market
https://docs.kalshi.com/api-reference/market/get-market-orderbook
  </read_first>
  <files>tests/unit/test_kalshi_client.py</files>
  <behavior>
    - Test 1: authenticated requests include the Kalshi access key, timestamp, and RSA-PSS signature headers.
    - Test 2: request signing uses the HTTP method and path, not the query string, as the signed payload.
    - Test 3: `list_markets`, `get_market`, and `get_market_orderbook` return project DTOs instead of raw transport dicts.
    - Test 4: the client rejects any write-oriented method or configuration that would expose order placement.
  </behavior>
  <action>Create the tests first so the client boundary is forced to stay read-only and normalized. Use small fixtures that assert exact header names, exact DTO fields, and exact transport-to-domain conversion behavior.</action>
  <verify>
    <automated>! python3 -m uv run pytest -q tests/unit/test_kalshi_client.py -x</automated>
  </verify>
  <done>The client tests exist and fail before the authenticated read client and DTOs are implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the authenticated read client and normalized DTO contracts</name>
  <read_first>
src/tennisprediction/config.py
src/tennisprediction/modeling/schemas.py
tests/unit/test_kalshi_client.py
https://docs.kalshi.com/getting_started/quick_start_authenticated_requests
https://docs.kalshi.com/sdks/overview
  </read_first>
  <files>src/tennisprediction/kalshi/__init__.py, src/tennisprediction/kalshi/schemas.py, src/tennisprediction/kalshi/client.py, tests/unit/test_kalshi_client.py</files>
  <action>Implement the project-owned client with explicit auth, request helpers, and read endpoint methods for market listing, market detail, and orderbook retrieval. Keep the DTOs frozen and typed, and keep all transport details behind the client class. Use direct `httpx` calls and Kalshi request signing rather than building business logic around a generated SDK object graph.</action>
  <verify>
    <automated>python3 -m uv run pytest -q tests/unit/test_kalshi_client.py -x</automated>
  </verify>
  <done>Phase 05 has a trusted read-only Kalshi client and normalized DTO boundary that business logic can consume safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config→client | Secrets and base URLs must be validated before any request is sent |
| transport→DTOs | Raw Kalshi payloads must be normalized immediately and not leaked upward |
| client→later phases | Read client must not expose write methods or execution-prep primitives |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Spoofing | `src/tennisprediction/kalshi/client.py` | mitigate | Sign every request with Kalshi headers and reject unsigned transport paths. |
| T-05-01-02 | Tampering | DTO normalization | mitigate | Freeze DTOs and normalize raw response data immediately at the client boundary. |
| T-05-01-03 | Elevation of Privilege | client surface | mitigate | Do not define or export any write-oriented methods, order builders, or execution staging helpers. |
</threat_model>

<verification>
- `! python3 -m uv run pytest -q tests/unit/test_kalshi_client.py -x`
- `python3 -m uv run ruff check src/tennisprediction/kalshi tests/unit/test_kalshi_client.py`
- `python3 -m uv run mypy src/tennisprediction/kalshi`
</verification>

<success_criteria>
- Developer can authenticate against Kalshi read endpoints through project-owned code.
- Client methods return normalized DTOs and do not leak raw transport objects into business logic.
- The client surface is read-only by construction.
</success_criteria>

<output>
Create `.planning/phases/05-kalshi-read-only-market-integration/05-01-SUMMARY.md` when done
</output>

