---
phase: 05-kalshi-read-only-market-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/tennisprediction/kalshi/storage.py
  - src/tennisprediction/kalshi/snapshots.py
  - tests/unit/test_kalshi_storage.py
autonomous: true
requirements:
  - KAL-02
must_haves:
  truths:
    - "System persists raw Kalshi market, market-detail, and orderbook snapshots with timestamps and request metadata."
    - "Snapshot storage remains repo-local, queryable, and auditable."
    - "Request metadata and payload lineage are persisted with the snapshot rows."
  artifacts:
    - path: "src/tennisprediction/kalshi/storage.py"
      provides: "DuckDB-backed snapshot persistence helpers for Kalshi read outputs"
      exports: ["persist_kalshi_snapshot_batch", "load_kalshi_snapshot_tables"]
    - path: "src/tennisprediction/kalshi/snapshots.py"
      provides: "Snapshot row and batch contracts for markets, details, orderbooks, and request metadata"
      exports: ["KalshiSnapshotBatch", "KalshiMarketSnapshotRow", "KalshiOrderbookSnapshotRow"]
    - path: "tests/unit/test_kalshi_storage.py"
      provides: "Regression coverage for persistence schema, timestamps, and metadata provenance"
  key_links:
    - from: "src/tennisprediction/kalshi/storage.py"
      to: "src/tennisprediction/storage/duckdb.py"
      via: "tabular snapshot persistence pattern"
      pattern: "_replace_table"
    - from: "src/tennisprediction/kalshi/storage.py"
      to: "src/tennisprediction/config.py"
      via: "repo-local storage path resolution"
      pattern: "Settings._resolve_repo_path"
    - from: "src/tennisprediction/kalshi/storage.py"
      to: "src/tennisprediction/modeling/reports.py"
      via: "thin file-writer and parquet/csv conventions"
      pattern: "_write_parquet"
---

<objective>
Create the persistence layer for raw Kalshi snapshot collection.
Output: repo-local storage helpers and typed snapshot rows that preserve request metadata, timestamps, and payload lineage for markets, market detail, and orderbooks.
</objective>

## Phase Goal

**As a** developer, **I want to** persist read-only Kalshi snapshots in a queryable local store, **so that** later mapping and EV phases can consume stable market data without replaying HTTP requests.

<execution_context>
@/Users/andrewlay/.codex/gsd-core/workflows/execute-plan.md
@/Users/andrewlay/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-kalshi-read-only-market-integration/05-RESEARCH.md
@.planning/phases/05-kalshi-read-only-market-integration/05-PATTERNS.md
@src/tennisprediction/config.py
@src/tennisprediction/storage/duckdb.py
@src/tennisprediction/modeling/reports.py
</context>

## Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Persist raw Kalshi snapshots and request metadata | 05-02 | COVERED | Storage is the phase-2 slice. |
| REQ | KAL-02 | Raw snapshot persistence with timestamps and request metadata | 05-02 | COVERED | DuckDB-backed tabular persistence. |
| RESEARCH | R-01 | Repo-local snapshot storage should remain auditable | 05-02 | COVERED | All paths stay inside the repository. |
| RESEARCH | R-02 | Existing code favors thin file writers and DuckDB tables | 05-02 | COVERED | Reuse that pattern directly. |
| CONTEXT | — | ATP-only, Kalshi-only, read-only boundaries remain in force | 05-02 | COVERED | No trading surfaces. |

## Artifacts this plan produces

- Files: `src/tennisprediction/kalshi/storage.py`, `src/tennisprediction/kalshi/snapshots.py`, `tests/unit/test_kalshi_storage.py`
- DTOs: `KalshiSnapshotBatch`, `KalshiMarketSnapshotRow`, `KalshiMarketDetailSnapshotRow`, `KalshiOrderbookSnapshotRow`, `KalshiRequestLogRow`
- Functions: `persist_kalshi_snapshot_batch`, `load_kalshi_snapshot_tables`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for snapshot persistence and request metadata</name>
  <read_first>
src/tennisprediction/config.py
src/tennisprediction/storage/duckdb.py
tests/unit/test_kalshi_client.py
  </read_first>
  <files>tests/unit/test_kalshi_storage.py</files>
  <behavior>
    - Test 1: market, market-detail, and orderbook snapshot rows are persisted with timestamps and request metadata.
    - Test 2: snapshot persistence preserves payload lineage fields such as request cursor, filter params, endpoint path, and response checksum.
    - Test 3: persisted tables can be reloaded from the repo-local DuckDB store without schema drift.
    - Test 4: the persistence layer rejects non-repo-local paths.
  </behavior>
  <action>Create the persistence tests first so the storage layer stays thin, deterministic, and repo-local. Use exact table names and exact column expectations to lock the snapshot contract down.</action>
  <verify>
    <automated>! python3 -m uv run pytest -q tests/unit/test_kalshi_storage.py -x</automated>
  </verify>
  <done>The persistence tests exist and fail before the snapshot store is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement DuckDB-backed Kalshi snapshot persistence</name>
  <read_first>
src/tennisprediction/config.py
src/tennisprediction/storage/duckdb.py
tests/unit/test_kalshi_storage.py
  </read_first>
  <files>src/tennisprediction/kalshi/storage.py, src/tennisprediction/kalshi/snapshots.py, tests/unit/test_kalshi_storage.py</files>
  <action>Implement a thin persistence helper that writes snapshot batches into repo-local DuckDB tables and carries request metadata, timestamps, and payload lineage through every row. Keep the data model explicit enough that later phases can query market history without rehydrating transport payloads.</action>
  <verify>
    <automated>python3 -m uv run pytest -q tests/unit/test_kalshi_storage.py -x</automated>
  </verify>
  <done>Phase 05 has a durable repo-local snapshot store for Kalshi market data and request metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→storage | Snapshot storage must preserve the original request metadata and response provenance |
| storage→later phases | Later phases must not need to re-fetch HTTP responses to understand a stored snapshot |
| repo path boundary | Snapshot files and the DuckDB database must remain within the repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | snapshot persistence | mitigate | Store timestamps, request metadata, and response checksums with every snapshot row. |
| T-05-02-02 | Repudiation | snapshot lineage | mitigate | Preserve endpoint path, cursor, and filter parameters so each row can be traced to the originating request. |
| T-05-02-03 | Information Disclosure | repo-local store | mitigate | Keep all data inside repo-local paths validated by `Settings._resolve_repo_path`. |
</threat_model>

<verification>
- `! python3 -m uv run pytest -q tests/unit/test_kalshi_storage.py -x`
- `python3 -m uv run ruff check src/tennisprediction/kalshi tests/unit/test_kalshi_storage.py`
- `python3 -m uv run mypy src/tennisprediction/kalshi`
</verification>

<success_criteria>
- Developer can persist Kalshi snapshots with request metadata and timestamps.
- Snapshot tables reload cleanly from the repo-local database.
- Storage remains a thin helper around typed rows and DuckDB.
</success_criteria>

<output>
Create `.planning/phases/05-kalshi-read-only-market-integration/05-02-SUMMARY.md` when done
</output>

