---
phase: 02-leakage-safe-feature-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tennisprediction/features/__init__.py
  - src/tennisprediction/features/schemas.py
  - src/tennisprediction/features/ordering.py
  - src/tennisprediction/features/rankings.py
  - src/tennisprediction/features/differential.py
  - src/tennisprediction/features/runner.py
  - tests/unit/test_feature_runner.py
  - tests/unit/test_feature_rankings.py
autonomous: true
requirements:
  - FEAT-01
  - FEAT-03
  - FEAT-07
  - FEAT-08
must_haves:
  truths:
    - "Developer can build one deterministic pre-match snapshot batch from canonical Phase 1 matches without using future rankings or same-round cross-updates."
    - "Every player-side snapshot exposes ranking and static match-context fields before any post-match state mutation."
    - "Developer can inspect one minimal A-versus-B differential row derived from player-side snapshots rather than reconstructed from raw matches."
  artifacts:
    - path: "src/tennisprediction/features/schemas.py"
      provides: "Feature snapshot and differential row contracts"
      exports: ["PlayerFeatureSnapshot", "FeatureDifferentialRow", "FeatureBuildResult"]
    - path: "src/tennisprediction/features/ordering.py"
      provides: "Deterministic date/round/source ordering and cohort partitioning"
      exports: ["ROUND_PRECEDENCE", "build_match_cohorts"]
    - path: "src/tennisprediction/features/rankings.py"
      provides: "Backward-only ranking lookup helpers"
      exports: ["attach_prior_rankings"]
    - path: "src/tennisprediction/features/runner.py"
      provides: "Chronological feature runner entrypoint"
      exports: ["build_feature_snapshots"]
  key_links:
    - from: "src/tennisprediction/features/runner.py"
      to: "src/tennisprediction/features/ordering.py"
      via: "cohort partitioning before snapshot emission"
      pattern: "build_match_cohorts"
    - from: "src/tennisprediction/features/runner.py"
      to: "src/tennisprediction/features/rankings.py"
      via: "backward ranking lookup"
      pattern: "attach_prior_rankings"
    - from: "src/tennisprediction/features/runner.py"
      to: "src/tennisprediction/features/differential.py"
      via: "player-side snapshot to A/B row derivation"
      pattern: "build_differential_row"
---

<objective>
Create the first working vertical slice of the Phase 2 feature engine: deterministic cohort ordering, backward-only ranking lookup, player-side snapshot contracts, and one minimal differential row per D-01, D-02, D-03, D-04, D-05, and D-06.

Purpose: Give later plans one concrete end-to-end path for building pre-match features from canonical Phase 1 tables without reopening the chronological leakage rules.
Output: Feature schemas, cohort ordering helpers, ranking-as-of lookup logic, a chronological runner, and failing-then-passing tests for the first happy path.
</objective>

## Phase Goal

**As a** developer, **I want to** build point-in-time ATP pre-match feature snapshots from canonical Phase 1 tables, **so that** future matches cannot contaminate historical model inputs.

<execution_context>
@/Users/andrewlay/.codex/gsd-core/workflows/execute-plan.md
@/Users/andrewlay/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-leakage-safe-feature-engine/02-CONTEXT.md
@.planning/phases/02-leakage-safe-feature-engine/02-RESEARCH.md
@.planning/phases/02-leakage-safe-feature-engine/02-PATTERNS.md
@.planning/phases/01-foundation-and-atp-data-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-and-atp-data-contracts/01-04-SUMMARY.md
@src/tennisprediction/domain/models.py
@src/tennisprediction/domain/normalization.py
@src/tennisprediction/storage/duckdb.py
@tests/unit/test_normalization.py
</context>

## Artifacts this phase produces

- Files: `src/tennisprediction/features/__init__.py`, `src/tennisprediction/features/schemas.py`, `src/tennisprediction/features/ordering.py`, `src/tennisprediction/features/rankings.py`, `src/tennisprediction/features/differential.py`, `src/tennisprediction/features/runner.py`, `tests/unit/test_feature_runner.py`, `tests/unit/test_feature_rankings.py`
- Dataclasses: `PlayerFeatureSnapshot`, `FeatureDifferentialRow`, `FeatureBuildResult`
- Constants: `ROUND_PRECEDENCE`
- Functions: `build_match_cohorts`, `attach_prior_rankings`, `build_differential_row`, `build_feature_snapshots`

<tasks>

<task type="auto">
  <name>Task 1: Write the failing happy-path tests for ranking-safe chronological snapshots</name>
  <read_first>
tests/unit/test_normalization.py
tests/unit/test_validation.py
src/tennisprediction/domain/models.py
src/tennisprediction/domain/normalization.py
.planning/phases/02-leakage-safe-feature-engine/02-PATTERNS.md
  </read_first>
  <files>tests/unit/test_feature_runner.py, tests/unit/test_feature_rankings.py</files>
  <action>Create failing tests named `test_build_feature_snapshots_emits_pre_match_rank_and_context_before_cohort_updates` and `test_attach_prior_rankings_uses_backward_as_of_only`. Build tiny canonical fixtures with two same-round matches on the same `tourney_date`, ranking rows before and after that date, and a stable `SourceLineage`. Assert per D-01 and D-04 that both matches in the cohort see the same pre-cohort baseline, per D-02 that only `ranking_date <= tourney_date` is selected, and per D-03 that ordering falls back to `tourney_date`, `ROUND_PRECEDENCE`, `lineage.source_file_path`, and `lineage.source_row_number`.</action>
  <acceptance_criteria>
- `tests/unit/test_feature_runner.py` contains `test_build_feature_snapshots_emits_pre_match_rank_and_context_before_cohort_updates`.
- `tests/unit/test_feature_rankings.py` contains `test_attach_prior_rankings_uses_backward_as_of_only`.
- `! uv run pytest tests/unit/test_feature_runner.py tests/unit/test_feature_rankings.py -q` exits non-zero before implementation because `build_feature_snapshots` and `attach_prior_rankings` do not yet satisfy the asserted behavior.
  </acceptance_criteria>
  <verify>
    <automated>! uv run pytest tests/unit/test_feature_runner.py tests/unit/test_feature_rankings.py -q</automated>
  </verify>
  <done>The Phase 2 happy-path tests exist and fail for the exact missing chronological and ranking-safe behaviors this slice is about to implement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the first end-to-end feature slice for ordering, rankings, context, and minimal differentials</name>
  <read_first>
src/tennisprediction/domain/__init__.py
src/tennisprediction/domain/models.py
src/tennisprediction/domain/normalization.py
src/tennisprediction/storage/duckdb.py
tests/unit/test_feature_runner.py
tests/unit/test_feature_rankings.py
.planning/phases/02-leakage-safe-feature-engine/02-CONTEXT.md
.planning/phases/02-leakage-safe-feature-engine/02-PATTERNS.md
  </read_first>
  <files>src/tennisprediction/features/__init__.py, src/tennisprediction/features/schemas.py, src/tennisprediction/features/ordering.py, src/tennisprediction/features/rankings.py, src/tennisprediction/features/differential.py, src/tennisprediction/features/runner.py, tests/unit/test_feature_runner.py, tests/unit/test_feature_rankings.py</files>
  <behavior>
    - Test 1: `build_feature_snapshots` emits player-side snapshots before any cohort result updates and returns one minimal differential row per accepted match.
    - Test 2: `attach_prior_rankings` uses only backward ranking rows and leaves missing rankings explicit instead of filtering out the match.
  </behavior>
  <action>Implement `PlayerFeatureSnapshot`, `FeatureDifferentialRow`, and `FeatureBuildResult` in `src/tennisprediction/features/schemas.py` with concrete fields `feature_version`, `canonical_match_id`, `canonical_player_id`, `opponent_canonical_player_id`, `player_a_id`, `player_b_id`, `as_of_date`, `side`, `surface`, `tourney_level`, `round_name`, `best_of`, `rank`, `rank_points`, `rank_missing`, `rank_points_missing`, `ranking_age_days`, and `lineage` per D-05 and D-08. Implement `ROUND_PRECEDENCE` and `build_match_cohorts` in `ordering.py` so ordering is `tourney_date` then round precedence then `source_file_path` and `source_row_number` per D-03, while same-round cohorts share one pre-round baseline per D-04. Implement `attach_prior_rankings` in `rankings.py` as a backward-only as-of lookup keyed by canonical player ID and `ranking_date <= tourney_date` per D-02. Implement `build_differential_row` and `build_feature_snapshots` so the runner emits player-side snapshots first per D-01, builds one minimal A/B row with `rank_diff` and static match-context fields per D-06 and FEAT-08, and keeps missing rankings explicit with nulls plus flags instead of excluding the match per D-08.</action>
  <acceptance_criteria>
- `src/tennisprediction/features/schemas.py` exports `PlayerFeatureSnapshot`, `FeatureDifferentialRow`, and `FeatureBuildResult`.
- `src/tennisprediction/features/ordering.py` defines `ROUND_PRECEDENCE` and `build_match_cohorts`.
- `src/tennisprediction/features/rankings.py` defines `attach_prior_rankings` and does not use forward or nearest ranking selection.
- `src/tennisprediction/features/runner.py` defines `build_feature_snapshots`.
- `uv run pytest tests/unit/test_feature_runner.py tests/unit/test_feature_rankings.py -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_feature_runner.py tests/unit/test_feature_rankings.py -q</automated>
  </verify>
  <done>The repository has a working chronological feature runner that emits ranking-safe player-side snapshots and minimal differential rows from canonical matches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical Phase 1 tables→feature runner | Only canonical, ATP-only, lineage-bearing rows should enter the feature pipeline |
| ranking history→point-in-time lookup | A wrong lookup strategy can leak future rankings into historical matches |
| player-side snapshots→differential rows | Reconstructing differentials from raw matches would bypass the single-source-of-truth contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | `ordering.py`, `runner.py` | mitigate | Centralize deterministic cohort ordering and assert same-round baseline behavior in `test_build_feature_snapshots_emits_pre_match_rank_and_context_before_cohort_updates`. |
| T-02-01-02 | Information Disclosure | `rankings.py` | accept | The phase uses public ranking data only; no secret-bearing inputs cross this boundary. |
| T-02-01-03 | Repudiation | `schemas.py`, `differential.py` | mitigate | Carry `feature_version`, match/player IDs, side orientation, and source lineage in every snapshot and row so downstream consumers never rebuild provenance ad hoc. |
</threat_model>

<verification>
- `! uv run pytest tests/unit/test_feature_runner.py tests/unit/test_feature_rankings.py -q`
- `uv run pytest tests/unit/test_feature_runner.py tests/unit/test_feature_rankings.py -q`
</verification>

<success_criteria>
- One public runner can emit deterministic pre-match snapshots from canonical matches.
- Ranking features use only backward-as-of data and keep missing rankings explicit.
- Minimal A/B rows come from player-side snapshots instead of from raw canonical tables.
</success_criteria>

<output>
Create `.planning/phases/02-leakage-safe-feature-engine/02-01-SUMMARY.md` when done
</output>
