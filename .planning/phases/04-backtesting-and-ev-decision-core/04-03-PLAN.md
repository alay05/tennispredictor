---
phase: 04-backtesting-and-ev-decision-core
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/tennisprediction/backtesting/metrics.py
  - src/tennisprediction/backtesting/provenance.py
  - src/tennisprediction/backtesting/reports.py
  - tests/unit/test_backtesting_metrics.py
  - tests/unit/test_backtesting_reports.py
autonomous: true
requirements:
  - BKT-04
  - BKT-05
  - BKT-06
must_haves:
  truths:
    - "Developer can inspect ROI, profit curve, win rate, average edge, max drawdown, gross profit, net profit, and sample size from chronological accepted opportunity records."
    - "Developer can inspect an explicit profitability-uncertainty surface, such as a confidence interval or bootstrap band, from chronological accepted opportunity records before any claim is shown."
    - "Every backtest report is labeled as `actual_kalshi_historical`, `collected_snapshot_replay`, or `synthetic_proxy` before any profitability claim is shown."
    - "Profitability summaries are guarded by provenance, sample-size, uncertainty, and assumption metadata, so unsupported claims are suppressed instead of printed as ordinary results."
  artifacts:
    - path: "src/tennisprediction/backtesting/metrics.py"
      provides: "Chronological backtest metrics, equity-curve, drawdown, and uncertainty calculations"
      exports: ["summarize_backtest", "estimate_backtest_uncertainty"]
    - path: "src/tennisprediction/backtesting/provenance.py"
      provides: "Closed provenance-label enum and profitability-claim guardrails"
      exports: ["BacktestProvenanceLabel", "guard_profitability_claims"]
    - path: "src/tennisprediction/backtesting/reports.py"
      provides: "Persisted accepted/rejected opportunity reports, reason counts, summary metrics, uncertainty payloads, equity curve, and provenance payloads"
      exports: ["write_backtest_reports"]
    - path: "tests/unit/test_backtesting_reports.py"
      provides: "Regression coverage for provenance labels and guarded profitability summaries"
  key_links:
    - from: "src/tennisprediction/backtesting/metrics.py"
      to: "src/tennisprediction/ev/opportunity.py"
      via: "calculates summary metrics only from accepted decision records in chronological order"
      pattern: "accepted|realized_pnl|edge"
    - from: "src/tennisprediction/backtesting/reports.py"
      to: "src/tennisprediction/config.py"
      via: "writes repo-local report artifacts under `reports/backtesting/<run_id>`"
      pattern: "reports_dir"
    - from: "src/tennisprediction/backtesting/reports.py"
      to: "src/tennisprediction/backtesting/provenance.py"
      via: "applies provenance banner and profitability-claim suppression before writing summary outputs"
      pattern: "provenance|claim"
---

<objective>
Create the third Phase 04 vertical slice: chronological betting metrics plus provenance and profitability-claim guardrails, closing BKT-04, BKT-05, and BKT-06.

Purpose: Turn accepted/rejected decision records into auditable backtest evidence that is clearly labeled and cannot overstate unsupported profitability results.
Output: Backtesting metrics, provenance guards, persisted backtest reports under `reports/backtesting/`, and failing-then-passing tests for metrics and reporting guardrails.
</objective>

## Phase Goal

**As a** developer, **I want to** replay persisted ATP predictions through a Kalshi-aligned EV core, **so that** I can judge opportunity quality without unsupported profitability claims.

<execution_context>
@/Users/andrewlay/.codex/gsd-core/workflows/execute-plan.md
@/Users/andrewlay/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-backtesting-and-ev-decision-core/04-RESEARCH.md
@.planning/phases/04-backtesting-and-ev-decision-core/04-PATTERNS.md
@.planning/phases/03-modeling-calibration-and-artifact-registry/03-04-SUMMARY.md
@src/tennisprediction/modeling/reports.py
@src/tennisprediction/modeling/metrics.py
@src/tennisprediction/ev/pricing.py
@src/tennisprediction/ev/opportunity.py
@src/tennisprediction/config.py
</context>

## Artifacts this plan produces

- Files: `src/tennisprediction/backtesting/metrics.py`, `src/tennisprediction/backtesting/provenance.py`, `src/tennisprediction/backtesting/reports.py`, `tests/unit/test_backtesting_metrics.py`, `tests/unit/test_backtesting_reports.py`
- Dataclasses / models: `BacktestProvenanceLabel`, `BacktestSummary`, `BacktestUncertainty`, `EquityCurvePoint`
- Functions: `summarize_backtest`, `estimate_backtest_uncertainty`, `guard_profitability_claims`, `write_backtest_reports`
- Persistent outputs: `reports/backtesting/<run_id>/summary.json`, `uncertainty.json`, `equity_curve.csv`, `accepted_opportunities.parquet`, `rejected_opportunities.parquet`, `decision_reason_counts.csv`, `provenance.json`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing tests for chronological backtest metrics and provenance guardrails</name>
  <read_first>
src/tennisprediction/modeling/metrics.py
src/tennisprediction/modeling/reports.py
src/tennisprediction/ev/opportunity.py
.planning/phases/04-backtesting-and-ev-decision-core/04-RESEARCH.md
.planning/phases/04-backtesting-and-ev-decision-core/04-PATTERNS.md
  </read_first>
  <files>tests/unit/test_backtesting_metrics.py, tests/unit/test_backtesting_reports.py</files>
  <behavior>
    - Test 1: `summarize_backtest` computes sample size, accepted/rejected counts, win rate, average edge, gross profit, net profit, ROI, chronological equity-curve rows, and max drawdown from accepted decisions in time order.
    - Test 2: `write_backtest_reports` persists accepted and rejected opportunity parquet files, `decision_reason_counts.csv`, `summary.json`, `uncertainty.json`, `equity_curve.csv`, and `provenance.json` under `reports/backtesting/<run_id>`.
    - Test 3: `guard_profitability_claims` suppresses profitability claims unless provenance label, sample size, uncertainty, and assumption notes are present, and synthetic/proxy runs carry an explicit limitation banner.
  </behavior>
  <action>Create failing tests that use small accepted/rejected decision fixtures and all three run-level provenance labels. In the metric tests, lock the exact chronological path behavior required for max drawdown by asserting the equity curve updates in row order rather than from unordered aggregates, and assert an uncertainty band or interval is returned for profitability summaries. In the report tests, assert the output payloads always carry `artifact_run_id`, `feature_version`, `split_manifest_id`, `provenance_label`, `sample_size`, `assumption_notes`, and uncertainty metadata, and that unsupported provenance or missing claim-guard fields stop profitability output from being written as an ordinary success summary.</action>
  <verify>
    <automated>! python3 -m uv run pytest -q tests/unit/test_backtesting_metrics.py tests/unit/test_backtesting_reports.py -x</automated>
  </verify>
  <done>The reporting/metrics tests exist, lock the required backtest metrics plus provenance guardrails, and fail before the final Phase 04 implementation slice is written.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement chronological backtest summaries, provenance gating, and persisted report outputs</name>
  <read_first>
src/tennisprediction/config.py
src/tennisprediction/modeling/metrics.py
src/tennisprediction/modeling/reports.py
tests/unit/test_backtesting_metrics.py
tests/unit/test_backtesting_reports.py
.planning/phases/04-backtesting-and-ev-decision-core/04-RESEARCH.md
.planning/phases/04-backtesting-and-ev-decision-core/04-PATTERNS.md
  </read_first>
  <files>src/tennisprediction/backtesting/metrics.py, src/tennisprediction/backtesting/provenance.py, src/tennisprediction/backtesting/reports.py, tests/unit/test_backtesting_metrics.py, tests/unit/test_backtesting_reports.py</files>
  <action>Follow the exact analogs in `04-PATTERNS.md`: keep `metrics.py` as a pure aggregator shaped after `src/tennisprediction/modeling/metrics.py`, keep `reports.py` as a thin file-writer shaped after `src/tennisprediction/modeling/reports.py`, and isolate provenance logic in `provenance.py` so report bannering is not hidden in metric math. Add a closed `BacktestProvenanceLabel` enum with `actual_kalshi_historical`, `collected_snapshot_replay`, and `synthetic_proxy`, then implement `summarize_backtest(...)` so it computes win rate, average edge, sample size, gross profit, net profit, ROI, chronological equity-curve rows, max drawdown, and an explicit uncertainty estimate from accepted decisions only. Implement `estimate_backtest_uncertainty(...)` so the profitability surface can expose a confidence interval or bootstrap band alongside the summary. Implement `guard_profitability_claims(...)` so profitability surfaces require provenance label, sample size, uncertainty, and assumption notes; synthetic/proxy runs must produce an explicit limitation banner instead of quiet ROI language. Implement `write_backtest_reports(...)` so it writes `accepted_opportunities.parquet`, `rejected_opportunities.parquet`, `decision_reason_counts.csv`, `summary.json`, `uncertainty.json`, `equity_curve.csv`, and `provenance.json` under `settings.reports_dir / "backtesting" / run_id`, using repo-local append-only directories in the same spirit as the Phase 03 modeling reports. Keep the phase read-only and Kalshi-only: no order execution artifacts, no venue-agnostic abstractions, and no unlabeled profitability statements.</action>
  <verify>
    <automated>python3 -m uv run pytest -q tests/unit/test_backtesting_replay.py tests/unit/test_backtesting_decisions.py tests/unit/test_backtesting_metrics.py tests/unit/test_backtesting_reports.py -x</automated>
  </verify>
  <done>Phase 04 can summarize chronological accepted decisions, write the required backtest report artifacts, and prevent unsupported profitability claims by enforcing provenance, uncertainty, and assumption guardrails on every report surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accepted/rejected decision records→metrics | Only chronological accepted decisions should drive ROI, equity-curve, and drawdown calculations |
| provenance label→profitability summary | Unsupported or unlabeled evidence must not be reported as a normal profitability claim |
| report writer→repo filesystem | Backtest outputs must land in deterministic repo-local paths with row-complete provenance artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Repudiation | `src/tennisprediction/backtesting/provenance.py` | mitigate | Require one closed provenance label plus assumption notes before any profitability claim is emitted, and banner synthetic/proxy limitations explicitly. |
| T-04-03-02 | Tampering | `src/tennisprediction/backtesting/metrics.py` | mitigate | Compute equity curve and max drawdown from chronological accepted decision rows only, then persist the curve so later readers cannot reinterpret unordered aggregates. |
| T-04-03-03 | Spoofing | `src/tennisprediction/backtesting/reports.py` | mitigate | Persist `provenance.json` and summary metadata alongside accepted/rejected rows so a report cannot masquerade as actual Kalshi historical evidence without the matching label. |
</threat_model>

<verification>
- `! python3 -m uv run pytest -q tests/unit/test_backtesting_metrics.py tests/unit/test_backtesting_reports.py -x`
- `python3 -m uv run pytest -q tests/unit/test_backtesting_replay.py tests/unit/test_backtesting_decisions.py tests/unit/test_backtesting_metrics.py tests/unit/test_backtesting_reports.py -x`
- `python3 -m uv run ruff check src/tennisprediction/backtesting src/tennisprediction/ev tests/unit/test_backtesting_metrics.py tests/unit/test_backtesting_reports.py`
- `python3 -m uv run mypy src/tennisprediction/backtesting src/tennisprediction/ev`
</verification>

<success_criteria>
- Developer can inspect chronological backtest metrics and an equity curve derived from accepted decision records, including max drawdown and ROI.
- Every report is labeled as `actual_kalshi_historical`, `collected_snapshot_replay`, or `synthetic_proxy`, with assumption notes attached before profitability metrics are presented.
- Unsupported or unlabeled profitability claims are suppressed rather than written into normal summary outputs.
</success_criteria>

<output>
Create `.planning/phases/04-backtesting-and-ev-decision-core/04-03-SUMMARY.md` when done
</output>
