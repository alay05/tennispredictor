---
phase: 04-backtesting-and-ev-decision-core
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/tennisprediction/ev/__init__.py
  - src/tennisprediction/ev/pricing.py
  - src/tennisprediction/ev/opportunity.py
  - tests/unit/test_backtesting_decisions.py
autonomous: true
requirements:
  - BKT-02
  - BKT-03
must_haves:
  truths:
    - "System applies one reusable, side-symmetric EV decision engine to every replayed ATP prediction row and every normalized Kalshi-only market input row."
    - "Accepted and rejected records both preserve selected side, confidence, market probability, edge, expected value, liquidity, threshold snapshot, provenance label, and reason codes."
    - "Decision evaluation fails closed on missing or invalid market probability, unsupported provenance, or insufficient liquidity instead of silently dropping rows."
  artifacts:
    - path: "src/tennisprediction/ev/pricing.py"
      provides: "Pure helpers for side alignment, edge, and expected-value calculations"
      exports: ["evaluate_candidate_side"]
    - path: "src/tennisprediction/ev/opportunity.py"
      provides: "Typed normalized market input, threshold, and accepted/rejected decision record contracts"
      exports: ["NormalizedMarketInput", "DecisionThresholds", "evaluate_opportunities"]
    - path: "tests/unit/test_backtesting_decisions.py"
      provides: "Regression coverage for selected side, threshold filtering, and reason-coded rejected rows"
  key_links:
    - from: "src/tennisprediction/ev/opportunity.py"
      to: "src/tennisprediction/backtesting/replay.py"
      via: "consumes replayed calibrated probabilities without reopening artifact or dataset logic"
      pattern: "ReplayPredictionRow|replay_model_predictions"
    - from: "src/tennisprediction/ev/pricing.py"
      to: "src/tennisprediction/ev/opportunity.py"
      via: "pure side-symmetric probability and EV helpers feed accepted/rejected record builders"
      pattern: "expected_value|edge|selected_side"
    - from: "src/tennisprediction/ev/opportunity.py"
      to: "reports/backtesting/<run_id>"
      via: "accepted/rejected records become the persisted reporting inputs for Phase 04-03"
      pattern: "accepted|rejected|reason"
---

<objective>
Create the second Phase 04 vertical slice: a reusable EV decision engine with explicit thresholding and reason-coded accepted/rejected opportunity records, closing BKT-02 and BKT-03.

Purpose: Convert replayed model probabilities into auditable Kalshi-only decision records before any profitability metrics or live-market integrations are layered on top.
Output: A new `tennisprediction.ev` package with normalized market-input contracts, side-symmetric EV math, threshold filtering, and failing-then-passing decision tests.
</objective>

## Phase Goal

**As a** developer, **I want to** replay persisted ATP predictions through a Kalshi-aligned EV core, **so that** I can judge opportunity quality without unsupported profitability claims.

<execution_context>
@/Users/andrewlay/.codex/gsd-core/workflows/execute-plan.md
@/Users/andrewlay/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-backtesting-and-ev-decision-core/04-RESEARCH.md
@.planning/phases/04-backtesting-and-ev-decision-core/04-PATTERNS.md
@.planning/phases/03-modeling-calibration-and-artifact-registry/03-03-SUMMARY.md
@.planning/phases/03-modeling-calibration-and-artifact-registry/03-04-SUMMARY.md
@src/tennisprediction/modeling/schemas.py
@src/tennisprediction/modeling/metrics.py
@src/tennisprediction/backtesting/replay.py
@src/tennisprediction/backtesting/schemas.py
</context>

## Artifacts this plan produces

- Files: `src/tennisprediction/ev/__init__.py`, `src/tennisprediction/ev/pricing.py`, `src/tennisprediction/ev/opportunity.py`, `tests/unit/test_backtesting_decisions.py`
- Dataclasses / models: `NormalizedMarketInput`, `DecisionThresholds`, `OpportunityDecisionRecord`, `OpportunityDecisionBatch`
- Functions: `evaluate_candidate_side`, `evaluate_opportunities`
- Explicit Phase 04 stance: decision math consumes a normalized positive-side `market_probability` scalar and a normalized `available_liquidity_dollars` scalar; raw Kalshi price-field and orderbook-field derivation is deferred to Phases 05-06

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing tests for side-symmetric EV math and reason-coded decision records</name>
  <read_first>
src/tennisprediction/backtesting/replay.py
src/tennisprediction/modeling/schemas.py
tests/unit/test_backtesting_replay.py
.planning/phases/04-backtesting-and-ev-decision-core/04-RESEARCH.md
.planning/phases/04-backtesting-and-ev-decision-core/04-PATTERNS.md
  </read_first>
  <files>tests/unit/test_backtesting_decisions.py</files>
  <behavior>
    - Test 1: One replay row plus one normalized market row evaluates both positive and negative sides, where `market_probability` is the positive/player-A probability and the negative side is derived as `1 - market_probability`, chooses the higher expected-value side, and records the chosen side’s confidence, edge, and expected value.
    - Test 2: Accepted rows pass `min_edge`, `min_confidence`, and `min_liquidity`, while rejected rows preserve one or more reason codes such as `below_min_edge`, `below_min_confidence`, `below_min_liquidity`, `missing_market_probability`, `invalid_probability_bounds`, or `unsupported_provenance`.
    - Test 3: Phase 04’s normalized-input stance is enforced: `market_probability` is the positive/player-A probability, `market_probability_source` is a closed enum describing the normalized pricing basis, `available_liquidity_dollars` and `liquidity_source` are required inputs, and `provenance_label` is a separate closed enum for replay provenance; no test depends on raw Kalshi payload fields.
  </behavior>
  <action>Create failing decision tests using small synthetic replay rows plus synthetic normalized market rows. Make the Phase 04 planning stance explicit in those tests: the decision engine consumes a positive-side `market_probability` scalar and a normalized `available_liquidity_dollars` scalar, with `market_probability_source` recording the pricing basis and `provenance_label` recording replay provenance, so the EV slice stays independent from raw Kalshi `yes_bid_dollars`, `yes_ask_dollars`, `last_price_dollars`, or orderbook parsing. Also assert the exact threshold snapshot and reason-code behavior needed by BKT-03 so accepted and rejected rows are equally auditable.</action>
  <verify>
    <automated>! python3 -m uv run pytest -q tests/unit/test_backtesting_decisions.py -x</automated>
  </verify>
  <done>The EV-decision tests exist, lock the normalized market-input contract plus reason-code semantics, and fail before the decision engine is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement normalized EV pricing, thresholds, and accepted/rejected opportunity batches</name>
  <read_first>
src/tennisprediction/backtesting/replay.py
src/tennisprediction/backtesting/schemas.py
tests/unit/test_backtesting_decisions.py
.planning/phases/04-backtesting-and-ev-decision-core/04-RESEARCH.md
.planning/phases/04-backtesting-and-ev-decision-core/04-PATTERNS.md
  </read_first>
  <files>src/tennisprediction/ev/__init__.py, src/tennisprediction/ev/pricing.py, src/tennisprediction/ev/opportunity.py, tests/unit/test_backtesting_decisions.py</files>
  <action>Follow the exact analogs in `04-PATTERNS.md`: keep `pricing.py` as a pure helper module shaped after `src/tennisprediction/features/differential.py`, and keep `opportunity.py` as the typed batch builder shaped after the explicit grouping/result style in `src/tennisprediction/modeling/metrics.py`. Add immutable decision contracts for normalized market inputs, thresholds, accepted records, rejected records, and one decision batch result. Adopt one explicit Phase 04 stance to resolve the research ambiguity: `market_probability_source` is a required closed enum that records the normalized pricing basis, with Phase 04 MVP fixtures using `normalized_positive_side`; `provenance_label` is a separate closed enum with `synthetic_proxy`, `collected_snapshot_replay`, and `actual_kalshi_historical`; `available_liquidity_dollars` is the canonical liquidity threshold input for this phase, already normalized before decision math, and must carry a `liquidity_source` label rather than being derived from raw orderbook fields here. Implement side-symmetric pricing helpers that compute the positive and negative side from one replayed model row, choose the higher EV side, apply thresholds plus any per-contract fee/slippage assumption from the threshold snapshot, and emit accepted plus rejected records with reason codes instead of silently discarding failures. Do not add HTTP client logic, Kalshi SDK parsing, or execution surfaces.</action>
  <verify>
    <automated>python3 -m uv run pytest -q tests/unit/test_backtesting_replay.py tests/unit/test_backtesting_decisions.py -x</automated>
  </verify>
  <done>Phase 04 has a reusable EV decision engine that turns replayed ATP probabilities plus normalized Kalshi-only market inputs into auditable accepted and rejected opportunity records with explicit threshold snapshots and reason codes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replayed probabilities→EV engine | Selected-side logic must align model and market probabilities to the same contract side |
| normalized market inputs→decision records | Missing or malformed probability/liquidity metadata must fail closed |
| decision engine→later reports | Accepted and rejected rows must preserve threshold decisions instead of silently collapsing failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | `src/tennisprediction/ev/pricing.py` | mitigate | Compute both positive and negative sides from the same replay row and persist `selected_side`, `market_probability_source`, and `liquidity_source` so side-orientation drift is visible and testable. |
| T-04-02-02 | Repudiation | `src/tennisprediction/ev/opportunity.py` | mitigate | Persist both accepted and rejected records with exact threshold snapshots and one or more reason codes instead of silently dropping rows. |
| T-04-02-03 | Denial of Service | normalized market input validation | mitigate | Reject missing probabilities, invalid bounds, unknown provenance labels, or missing liquidity metadata before EV math so malformed replay inputs cannot poison downstream reports. |
</threat_model>

<verification>
- `! python3 -m uv run pytest -q tests/unit/test_backtesting_decisions.py -x`
- `python3 -m uv run pytest -q tests/unit/test_backtesting_replay.py tests/unit/test_backtesting_decisions.py -x`
- `python3 -m uv run ruff check src/tennisprediction/backtesting src/tennisprediction/ev tests/unit/test_backtesting_decisions.py`
- `python3 -m uv run mypy src/tennisprediction/backtesting src/tennisprediction/ev`
</verification>

<success_criteria>
- System evaluates both sides of every replayed ATP prediction row from one normalized market input row and persists the chosen side’s confidence, edge, EV, and liquidity fields.
- Accepted and rejected records both preserve provenance labels, threshold snapshots, and explicit reason codes.
- Phase 04 keeps market probability and liquidity as normalized inputs with explicit metadata, while raw Kalshi field derivation stays deferred to the later Kalshi phases.
</success_criteria>

<output>
Create `.planning/phases/04-backtesting-and-ev-decision-core/04-02-SUMMARY.md` when done
</output>
