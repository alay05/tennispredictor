---
phase: 03-modeling-calibration-and-artifact-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tennisprediction/modeling/__init__.py
  - src/tennisprediction/modeling/schemas.py
  - src/tennisprediction/modeling/datasets.py
  - src/tennisprediction/modeling/splits.py
  - tests/unit/modeling_fixtures.py
  - tests/unit/test_modeling_datasets.py
  - tests/unit/test_modeling_splits.py
autonomous: true
requirements:
  - MOD-01
must_haves:
  truths:
    - "Developer can materialize ordered ATP-only modeling rows from persisted Phase 02 differential outputs plus canonical match winners without recalculating tennis features."
    - "Developer can freeze one non-overlapping train/validation/test contract with exact match membership, row counts, and membership hashes."
    - "Every downstream trainer can load the same feature-column order and split membership from one manifest instead of regenerating holdouts ad hoc."
  artifacts:
    - path: "src/tennisprediction/modeling/schemas.py"
      provides: "Typed Phase 03 dataset and split contracts"
      exports: ["ModelingRow", "FrozenModelingDataset", "FrozenSplitWindow", "FrozenSplitManifest"]
    - path: "src/tennisprediction/modeling/datasets.py"
      provides: "DuckDB-backed modeling dataset materializer over Phase 02 persisted features"
      exports: ["materialize_modeling_dataset"]
    - path: "src/tennisprediction/modeling/splits.py"
      provides: "Chronological split freezer and manifest persistence"
      exports: ["SplitBoundaryConfig", "freeze_chronological_splits", "load_split_manifest"]
    - path: "tests/unit/test_modeling_datasets.py"
      provides: "Regression coverage for label joins and deterministic feature-column ordering"
    - path: "tests/unit/test_modeling_splits.py"
      provides: "Regression coverage for chronological split freezing and membership hashes"
  key_links:
    - from: "src/tennisprediction/modeling/datasets.py"
      to: "src/tennisprediction/features/persistence.py"
      via: "reads only `feature_differential_rows` plus canonical match winners"
      pattern: "feature_differential_rows"
    - from: "src/tennisprediction/modeling/datasets.py"
      to: "src/tennisprediction/storage/duckdb.py"
      via: "DuckDB query over persisted Phase 01/02 tables"
      pattern: "duckdb.connect"
    - from: "src/tennisprediction/modeling/splits.py"
      to: "src/tennisprediction/ingestion/manifests.py"
      via: "frozen manifest style and sha256 membership hashing"
      pattern: "ConfigDict\\(frozen=True\\)|sha256"
---

<objective>
Create the first working Phase 03 vertical slice: one label-ready ATP modeling dataset and one frozen chronological split manifest built directly from persisted Phase 02 outputs, covering MOD-01 without reopening feature computation.

Purpose: Give every later training/calibration plan one replayable dataset contract and one official train/validation/test membership surface.
Output: A new `tennisprediction.modeling` package with dataset/split contracts, DuckDB-backed dataset loading, immutable split manifests under `models/splits/`, and failing-then-passing unit tests for label joins and chronological freezing.
</objective>

## Phase Goal

**As a** developer, **I want to** train, calibrate, evaluate, and persist ATP match models, **so that** they produce trustworthy chronological win probabilities.

<execution_context>
@/Users/andrewlay/.codex/gsd-core/workflows/execute-plan.md
@/Users/andrewlay/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-modeling-calibration-and-artifact-registry/03-RESEARCH.md
@.planning/phases/03-modeling-calibration-and-artifact-registry/03-PATTERNS.md
@.planning/phases/02-leakage-safe-feature-engine/02-VERIFICATION.md
@.planning/phases/02-leakage-safe-feature-engine/02-04-SUMMARY.md
@.planning/phases/02-leakage-safe-feature-engine/02-05-SUMMARY.md
@src/tennisprediction/config.py
@src/tennisprediction/features/persistence.py
@src/tennisprediction/ingestion/manifests.py
@src/tennisprediction/storage/duckdb.py
@tests/unit/test_feature_persistence.py
</context>

## Artifacts this plan produces

- Files: `src/tennisprediction/modeling/__init__.py`, `src/tennisprediction/modeling/schemas.py`, `src/tennisprediction/modeling/datasets.py`, `src/tennisprediction/modeling/splits.py`, `tests/unit/modeling_fixtures.py`, `tests/unit/test_modeling_datasets.py`, `tests/unit/test_modeling_splits.py`
- Dataclasses / models: `ModelingRow`, `FrozenModelingDataset`, `FrozenSplitWindow`, `FrozenSplitManifest`
- Functions: `materialize_modeling_dataset`, `freeze_chronological_splits`, `load_split_manifest`
- Persistent outputs: split manifests under `models/splits/<split_id>.json`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing end-to-end tests for Phase 02 dataset loading and chronological split freezing</name>
  <read_first>
src/tennisprediction/features/persistence.py
src/tennisprediction/ingestion/manifests.py
src/tennisprediction/storage/duckdb.py
tests/unit/test_feature_persistence.py
.planning/phases/03-modeling-calibration-and-artifact-registry/03-PATTERNS.md
  </read_first>
  <files>tests/unit/modeling_fixtures.py, tests/unit/test_modeling_datasets.py, tests/unit/test_modeling_splits.py</files>
  <behavior>
    - Test 1: `materialize_modeling_dataset` loads only persisted `feature_differential_rows`, joins `canonical_matches` for the binary target, and preserves deterministic chronological row order without recomputing tennis features.
    - Test 2: `feature_columns` excludes label and identity fields, keeps one stable ordered feature list, and carries forward the persisted `feature_version` plus source lineage metadata.
    - Test 3: `freeze_chronological_splits` partitions the ordered rows into train, validation, and test windows using the first official chronological `70/15/15` policy resolved in `03-RESEARCH.md`, persists the resolved inclusive end-date boundaries plus ordered `canonical_match_id` memberships, row counts, and sha256 hashes, and rejects overlap or empty windows.
  </behavior>
  <action>Mirror the deterministic fixture shape and persisted-row assertions from `tests/unit/test_feature_persistence.py` for `tests/unit/modeling_fixtures.py` and `tests/unit/test_modeling_datasets.py`, because `03-PATTERNS.md` maps `src/tennisprediction/modeling/datasets.py` to `src/tennisprediction/features/persistence.py`. Create one reusable synthetic fixture module that builds a temporary DuckDB database containing Phase 02-style `feature_differential_rows` and the matching `canonical_matches` winner labels. In `tests/unit/test_modeling_datasets.py`, add failing coverage that asserts the dataset builder uses `player_a_id == winner_canonical_player_id` as the label definition, orders rows by `as_of_date`, `lineage_source_file_path`, `lineage_source_row_number`, and `canonical_match_id`, and never rebuilds Elo, form, H2H, rankings, or stat aggregates from raw Phase 01 tables. For `tests/unit/test_modeling_splits.py`, follow the frozen-manifest and membership-hash expectation style from `tests/unit/test_manifests.py`, because `03-PATTERNS.md` maps `src/tennisprediction/modeling/splits.py` to `src/tennisprediction/ingestion/manifests.py`. Add failing coverage that asserts a `SplitBoundaryConfig(train_end_date, validation_end_date, test_end_date)` contract produces non-overlapping memberships, exact ordered `canonical_match_id` lists, row counts, and membership hashes, and raises when a split window is empty or when validation/test boundaries are not strictly increasing.</action>
  <verify>
    <automated>! python3 -m uv run pytest -q tests/unit/test_modeling_datasets.py tests/unit/test_modeling_splits.py -x</automated>
  </verify>
  <done>The Phase 03 dataset and split tests exist, they assert the exact persisted-feature and chronological-freeze behavior this slice needs, and they fail before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the frozen modeling dataset and split-manifest slice</name>
  <read_first>
src/tennisprediction/config.py
src/tennisprediction/features/persistence.py
src/tennisprediction/ingestion/manifests.py
src/tennisprediction/storage/duckdb.py
tests/unit/modeling_fixtures.py
tests/unit/test_modeling_datasets.py
tests/unit/test_modeling_splits.py
.planning/phases/03-modeling-calibration-and-artifact-registry/03-RESEARCH.md
.planning/phases/03-modeling-calibration-and-artifact-registry/03-PATTERNS.md
  </read_first>
  <files>src/tennisprediction/modeling/__init__.py, src/tennisprediction/modeling/schemas.py, src/tennisprediction/modeling/datasets.py, src/tennisprediction/modeling/splits.py, tests/unit/modeling_fixtures.py, tests/unit/test_modeling_datasets.py, tests/unit/test_modeling_splits.py</files>
  <action>Create the `src/tennisprediction/modeling/` package and follow the exact analogs named in `03-PATTERNS.md`: shape `modeling/__init__.py` after `src/tennisprediction/features/__init__.py`, shape `modeling/schemas.py` after the frozen dataclass contracts in `src/tennisprediction/features/schemas.py` plus the frozen Pydantic manifest style in `src/tennisprediction/ingestion/manifests.py`, shape `modeling/datasets.py` after the deterministic row-building helpers in `src/tennisprediction/features/persistence.py`, and shape `modeling/splits.py` after the checksum and immutable-manifest helpers in `src/tennisprediction/ingestion/manifests.py`. In `schemas.py`, define immutable dataset/split contracts: `ModelingRow` for one persisted differential row plus label and metadata, `FrozenModelingDataset` for the ordered rows and one shared `feature_columns` list, `FrozenSplitWindow` for one split’s ordered match membership, row count, first/last `as_of_date`, and `membership_sha256`, and `FrozenSplitManifest` as a frozen Pydantic model patterned after `SourceManifest`. In `datasets.py`, implement `materialize_modeling_dataset(database_path, feature_version)` so it queries only `feature_differential_rows` joined to `canonical_matches`, derives `target = 1` when `player_a_id` won and `0` otherwise, keeps ATP-only persisted metadata intact, and computes one stable feature-column order by excluding identity, label, and provenance fields rather than by guessing column families. In `splits.py`, implement `SplitBoundaryConfig(train_end_date, validation_end_date, test_end_date)`, `freeze_chronological_splits(dataset, boundary_config, settings)`, and `load_split_manifest(path)` so the official holdout contract is persisted under `settings.models_dir / "splits" / "<split_id>.json"` with exact ordered `canonical_match_id` lists, row counts, and sha256 hashes over the ordered memberships. For the first official manifest, derive the inclusive boundary dates from the resolved chronological `70/15/15` policy in `03-RESEARCH.md` after ordering the dataset, then persist those resolved dates and memberships in the manifest. Do not recompute any tennis features in this slice; every modeling row must come from persisted Phase 02 columns only.</action>
  <verify>
    <automated>python3 -m uv run pytest -q tests/unit/test_modeling_datasets.py tests/unit/test_modeling_splits.py -x</automated>
  </verify>
  <done>The repo can materialize one label-ready modeling dataset from persisted Phase 02 rows and persist one immutable chronological split manifest that later plans can load directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted Phase 02 tables→dataset loader | Only stored Phase 02 rows plus canonical match winners should shape the modeling dataset |
| split-boundary config→frozen split manifest | A bad boundary or hidden reshuffle here contaminates every downstream metric |
| split manifest JSON→later trainers | Later plans trust this manifest as the one official holdout contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | `datasets.py` | mitigate | Query only `feature_differential_rows` plus `canonical_matches`, assert deterministic ordering in unit tests, and forbid any feature-family recomputation in the dataset builder. |
| T-03-01-02 | Repudiation | `splits.py`, split manifests | mitigate | Persist exact ordered match memberships, row counts, and sha256 hashes for every split window so later runs can prove identical holdouts. |
| T-03-01-03 | Denial of Service | `splits.py` | mitigate | Raise on empty or non-increasing split windows instead of silently creating degenerate train/validation/test sets. |
</threat_model>

<verification>
- `! python3 -m uv run pytest -q tests/unit/test_modeling_datasets.py tests/unit/test_modeling_splits.py -x`
- `python3 -m uv run pytest -q tests/unit/test_modeling_datasets.py tests/unit/test_modeling_splits.py -x`
- `python3 -m uv run ruff check src/tennisprediction/modeling tests/unit/modeling_fixtures.py tests/unit/test_modeling_datasets.py tests/unit/test_modeling_splits.py`
- `python3 -m uv run mypy src/tennisprediction/modeling`
</verification>

<success_criteria>
- One dataset loader returns deterministic ATP-only modeling rows sourced entirely from persisted Phase 02 differential features plus canonical match labels.
- One split freezer persists the official train/validation/test contract with exact memberships, row counts, and hashes.
- Later training code can consume split manifests without re-deriving holdouts from raw dates alone.
</success_criteria>

<output>
Create `.planning/phases/03-modeling-calibration-and-artifact-registry/03-01-SUMMARY.md` when done
</output>
