---
phase: 03-modeling-calibration-and-artifact-registry
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - pyproject.toml
  - uv.lock
  - src/tennisprediction/modeling/schemas.py
  - src/tennisprediction/modeling/baselines.py
  - tests/unit/test_modeling_baselines.py
autonomous: false
requirements:
  - MOD-02
  - MOD-03
must_haves:
  truths:
    - "Developer can approve and install the exact ML dependencies required for baseline training before any package-manager sync mutates the environment."
    - "Developer can fit logistic regression and random forest ATP baselines from the frozen Phase 03 dataset and split manifest without shuffling or fitting on validation/test rows."
    - "Each baseline trainer emits raw validation and test probabilities plus ordered feature-column and parameter metadata, and preserves the trained estimator handle or canonical raw-model artifact reference so later calibration and registry code does not infer or reconstruct it."
  artifacts:
    - path: "pyproject.toml"
      provides: "Phase 03 `ml` dependency group for the approved baseline/candidate stack"
    - path: "src/tennisprediction/modeling/schemas.py"
      provides: "Typed raw-fit result contract shared by all trainers"
      exports: ["RawModelFitResult"]
    - path: "src/tennisprediction/modeling/baselines.py"
      provides: "Logistic regression and random forest baseline trainers"
      exports: ["fit_logistic_regression_baseline", "fit_random_forest_baseline"]
    - path: "tests/unit/test_modeling_baselines.py"
      provides: "Regression coverage for train-only fitting and raw probability outputs"
  key_links:
    - from: "src/tennisprediction/modeling/baselines.py"
      to: "src/tennisprediction/modeling/splits.py"
      via: "manifest-driven train/validation/test membership selection"
      pattern: "split_manifest"
    - from: "src/tennisprediction/modeling/baselines.py"
      to: "pyproject.toml"
      via: "approved sklearn/joblib runtime dependency group"
      pattern: "scikit-learn|joblib"
    - from: "src/tennisprediction/modeling/baselines.py"
      to: "src/tennisprediction/modeling/schemas.py"
      via: "structured raw-fit results for later calibration/registry work"
      pattern: "RawModelFitResult"
---

<objective>
Create the second Phase 03 vertical slice: approve/install the required ML packages and train the two benchmark baselines on the frozen dataset contract, covering MOD-02 and MOD-03 while keeping Phase 03’s calibration boundary intact for later plans.

Purpose: Turn the frozen dataset/split slice into usable benchmark model outputs without leaking validation or test rows into model fitting.
Output: An approved `ml` dependency group, updated lockfile, typed raw-fit result contract that preserves the trained estimator for downstream calibration and artifact persistence, baseline trainer module, and failing-then-passing tests for logistic regression and random forest training.
</objective>

## Phase Goal

**As a** developer, **I want to** train, calibrate, evaluate, and persist ATP match models, **so that** they produce trustworthy chronological win probabilities.

<execution_context>
@/Users/andrewlay/.codex/gsd-core/workflows/execute-plan.md
@/Users/andrewlay/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-modeling-calibration-and-artifact-registry/03-RESEARCH.md
@.planning/phases/03-modeling-calibration-and-artifact-registry/03-PATTERNS.md
@.planning/phases/03-modeling-calibration-and-artifact-registry/03-01-SUMMARY.md
@pyproject.toml
@src/tennisprediction/config.py
@src/tennisprediction/modeling/schemas.py
@tests/unit/modeling_fixtures.py
</context>

## Artifacts this plan produces

- Files: `pyproject.toml`, `uv.lock`, `src/tennisprediction/modeling/schemas.py`, `src/tennisprediction/modeling/baselines.py`, `tests/unit/test_modeling_baselines.py`
- Dataclasses: `RawModelFitResult`
- Functions: `fit_logistic_regression_baseline`, `fit_random_forest_baseline`
- Environment change: approved `ml` dependency group synced through `python3 -m uv`

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve the Phase 03 ML dependency list before any install or lockfile update</name>
  <files>pyproject.toml, uv.lock</files>
  <action>Review and approve the exact modeling packages that this phase will install before the executor runs `python3 -m uv sync --group dev --group ml`. The research artifact contains a required `## Package Legitimacy Audit` table and flagged every new ML package as `SUS`, so installation must remain blocked until the human gate explicitly approves or edits the list. Keep the Phase 03 mandatory dependency surface limited to `pandas`, `scikit-learn`, `xgboost`, and `joblib`; do not install `mlflow` or `matplotlib` in this plan because the project-owned filesystem registry and calibration-bin CSV/JSON outputs remain canonical in this phase.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path

research = Path(".planning/phases/03-modeling-calibration-and-artifact-registry/03-RESEARCH.md").read_text()
required = ["## Package Legitimacy Audit", "`pandas`", "`scikit-learn`", "`xgboost`", "`joblib`"]
missing = [item for item in required if item not in research]
if missing:
    raise SystemExit(f"Missing package-gate prerequisites in 03-RESEARCH.md: {missing}")
print("package-gate prerequisites present")
PY</automated>
  </verify>
  <what-built>The baseline-training slice needs approved package-manager access to `pandas`, `scikit-learn`, `xgboost`, and `joblib`, all of which were marked `SUS` by the package-legitimacy seam in `03-RESEARCH.md`. This checkpoint exists solely to validate those packages before any environment mutation occurs.</what-built>
  <how-to-verify>
    1. Review the official package pages or repositories for `pandas`, `scikit-learn`, `xgboost`, and `joblib`.
    2. Confirm those four packages are acceptable for this repo and that `mlflow` / `matplotlib` stay out of the mandatory Phase 03 install set.
    3. Resume only after explicitly approving the list or naming replacements.
  </how-to-verify>
  <resume-signal>Type `approved` or name the replacement packages before any `uv sync` runs.</resume-signal>
  <done>The ML dependency set is explicitly approved or amended, and the executor has a clear green light for the Phase 03 environment sync.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the approved ML dependency group and write the failing baseline trainer tests</name>
  <read_first>
pyproject.toml
tests/unit/modeling_fixtures.py
src/tennisprediction/modeling/schemas.py
.planning/phases/03-modeling-calibration-and-artifact-registry/03-RESEARCH.md
.planning/phases/03-modeling-calibration-and-artifact-registry/03-PATTERNS.md
  </read_first>
  <files>pyproject.toml, uv.lock, tests/unit/test_modeling_baselines.py</files>
  <behavior>
    - Test 1: `fit_logistic_regression_baseline` uses only the split manifest’s train membership to fit the estimator and returns raw validation/test probabilities aligned to the manifest order.
    - Test 2: `fit_random_forest_baseline` uses the same ordered feature contract, never shuffles rows, and emits structured metadata describing params, split path, feature columns, and the trained estimator handle or canonical raw-model artifact reference needed later.
    - Test 3: the repo can sync the approved `ml` dependency group through `python3 -m uv sync --group dev --group ml` before baseline tests run.
  </behavior>
  <action>After Task 1 is approved, follow the existing dependency-group pattern in `pyproject.toml` to add an `ml` group using the Phase 03 project target bands for `pandas`, `scikit-learn`, `xgboost`, and `joblib`, then refresh `uv.lock` with `python3 -m uv sync --group dev --group ml`. Do not add `mlflow` or `matplotlib` here. For `tests/unit/test_modeling_baselines.py`, mirror the focused fixture-driven assertions from `tests/unit/test_feature_persistence.py`, because `03-PATTERNS.md` maps the modeling test modules back to those repo test patterns. Write failing coverage that loads the Phase 03 fixture dataset/split manifest, calls `fit_logistic_regression_baseline` and `fit_random_forest_baseline`, and asserts both trainers use manifest memberships instead of date re-derivation, fit on train rows only, and return raw validation/test probabilities plus explicit `feature_columns`, `model_params`, `split_manifest_path`, row-count metadata, and the trained estimator handle or canonical raw-model artifact reference required by Plans 03-03 and 03-04.</action>
  <verify>
    <automated>! python3 -m uv run pytest -q tests/unit/test_modeling_baselines.py -x</automated>
  </verify>
  <done>The approved ML dependency group is synced into the local environment, and the baseline trainer tests fail for the exact train-only behavior this slice is about to implement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the logistic regression and random forest baseline trainers</name>
  <read_first>
pyproject.toml
src/tennisprediction/modeling/schemas.py
src/tennisprediction/modeling/datasets.py
src/tennisprediction/modeling/splits.py
tests/unit/test_modeling_baselines.py
.planning/phases/03-modeling-calibration-and-artifact-registry/03-RESEARCH.md
.planning/phases/03-modeling-calibration-and-artifact-registry/03-PATTERNS.md
  </read_first>
  <files>pyproject.toml, uv.lock, src/tennisprediction/modeling/schemas.py, src/tennisprediction/modeling/baselines.py, tests/unit/test_modeling_baselines.py</files>
  <action>Extend `src/tennisprediction/modeling/schemas.py` by following the aggregate result-contract style in `src/tennisprediction/features/schemas.py`: add a frozen `RawModelFitResult` that carries `model_name`, `model_family`, ordered `feature_columns`, `split_manifest_path`, train/validation/test row counts, raw validation/test probabilities, estimator parameters, the trained estimator handle or canonical raw-model artifact reference, and any fit-time metadata needed by later calibration/registry work. Implement `fit_logistic_regression_baseline(dataset, split_manifest)` and `fit_random_forest_baseline(dataset, split_manifest)` in `baselines.py` by following the stepwise orchestration style in `src/tennisprediction/features/runner.py`: select rows strictly from the persisted split-manifest memberships, build feature matrices in one stable order, fit only on train rows, and emit raw probabilities for validation/test in manifest order. Make the cross-plan contract explicit that `RawModelFitResult` is the object Plan 03-03 uses for `FrozenEstimator`-style calibration and Plan 03-04 uses to persist the raw estimator artifact. Use a median-impute + standard-scale + `LogisticRegression(max_iter=1000, random_state=42)` pipeline for logistic regression and `RandomForestClassifier(n_estimators=400, random_state=42, n_jobs=-1)` on the same feature-column order for random forest. Keep calibration out of this slice entirely; the output should be raw fitted baselines ready for the next plan’s shared calibrator.</action>
  <verify>
    <automated>python3 -m uv run pytest -q tests/unit/test_modeling_datasets.py tests/unit/test_modeling_splits.py tests/unit/test_modeling_baselines.py -x</automated>
  </verify>
  <done>The repo can fit the two baseline models on the frozen Phase 03 dataset/split contract and produce structured raw-fit outputs without any holdout leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved dependency list→package resolver | Unreviewed third-party ML packages could enter the environment here |
| split manifest→baseline trainer | A trainer that ignores manifest membership can leak validation/test rows into fitting |
| raw fitted estimators→later calibration layer | Later plans trust the baseline outputs to be raw, train-only, and feature-order stable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | `baselines.py` | mitigate | Drive row selection strictly from `FrozenSplitManifest` memberships and lock train-only fitting through unit tests for both baseline trainers. |
| T-03-02-02 | Repudiation | `RawModelFitResult` | mitigate | Persist split path, row counts, ordered feature columns, and model params in every raw-fit result so later calibration/registry code never guesses provenance. |
| T-03-02-SC | Tampering | Python package installs | mitigate | Keep the install path behind a blocking human legitimacy checkpoint and install only the approved `ml` group packages. |
</threat_model>

<verification>
 - `python3 - <<'PY'
from pathlib import Path

research = Path(".planning/phases/03-modeling-calibration-and-artifact-registry/03-RESEARCH.md").read_text()
required = ["## Package Legitimacy Audit", "`pandas`", "`scikit-learn`", "`xgboost`", "`joblib`"]
missing = [item for item in required if item not in research]
if missing:
    raise SystemExit(f"Missing package-gate prerequisites in 03-RESEARCH.md: {missing}")
print("package-gate prerequisites present")
PY`
- `python3 -m uv sync --group dev --group ml`
- `! python3 -m uv run pytest -q tests/unit/test_modeling_baselines.py -x`
- `python3 -m uv run pytest -q tests/unit/test_modeling_datasets.py tests/unit/test_modeling_splits.py tests/unit/test_modeling_baselines.py -x`
- `python3 -m uv run ruff check src/tennisprediction/modeling tests/unit/test_modeling_baselines.py`
- `python3 -m uv run mypy src/tennisprediction/modeling`
</verification>

<success_criteria>
- The local environment contains the approved Phase 03 ML packages and nothing extra from the optional secondary stack.
- Logistic regression and random forest baselines fit strictly on train memberships from the frozen split manifest.
- Raw baseline outputs carry ordered feature-column, parameter, split metadata, and the trained estimator handle or canonical raw-model artifact reference for later calibration and registry steps.
</success_criteria>

<output>
Create `.planning/phases/03-modeling-calibration-and-artifact-registry/03-02-SUMMARY.md` when done
</output>
