---
phase: 06-market-mapping-executable-pricing-and-live-ev-monitor
plan: 04
type: execute
wave: 4
depends_on:
  - 06-02
  - 06-03
files_modified:
  - src/tennisprediction/backtesting/replay.py
  - src/tennisprediction/monitoring/__init__.py
  - src/tennisprediction/monitoring/scan.py
  - src/tennisprediction/monitoring/reports.py
  - src/tennisprediction/cli.py
  - tests/unit/test_live_scan_orchestration.py
  - tests/unit/test_live_monitor_reports.py
autonomous: false
requirements:
  - MKT-07
  - MKT-08
must_haves:
  truths:
    - "Operator can run a shadow-mode Kalshi scan that reuses trusted feature and model interfaces, resolves mappings, derives executable inputs, and emits accepted plus rejected outputs per D-15 and D-16."
    - "An optional live-readonly path can collect fresh snapshots first but still never exposes order-placement or execution surfaces."
    - "Accepted opportunities are ranked by expected value, edge, executable liquidity, confidence, and freshness, while operator-facing outputs also carry `mapping_state` and deterministic `mapping_confidence` tiers per D-17 and D-18."
    - "This slice restores the ML runtime needed for real trusted-artifact replay before claiming MKT-07 is delivered."
  artifacts:
    - path: "src/tennisprediction/backtesting/replay.py"
      provides: "Reusable prediction helper for arbitrary canonical match IDs loaded from trusted artifacts"
      exports: ["predict_matches_for_canonical_ids"]
    - path: "src/tennisprediction/monitoring/scan.py"
      provides: "Shadow-mode and live-readonly scan orchestration"
      exports: ["run_kalshi_ev_scan"]
    - path: "src/tennisprediction/monitoring/reports.py"
      provides: "Ranked monitor outputs plus persisted accepted/rejected scan reports with mapping-state/confidence columns"
      exports: ["write_live_monitor_reports"]
    - path: "tests/unit/test_live_scan_orchestration.py"
      provides: "Regression coverage for trusted artifact reuse, mapping gating, and optional fresh collection"
    - path: "tests/unit/test_live_monitor_reports.py"
      provides: "Regression coverage for ranking order and rejected/excluded summary outputs"
  key_links:
    - from: "src/tennisprediction/monitoring/scan.py"
      to: "src/tennisprediction/market_mapping/resolver.py"
      via: "only matched mappings are allowed to proceed to executable pricing"
      pattern: "resolve_kalshi_market_mappings|require_matched_mapping"
    - from: "src/tennisprediction/monitoring/scan.py"
      to: "src/tennisprediction/backtesting/replay.py"
      via: "trusted artifact bundle and feature interface reuse for mapped canonical matches"
      pattern: "predict_matches_for_canonical_ids|ReplayPredictionRow"
    - from: "src/tennisprediction/monitoring/reports.py"
      to: "src/tennisprediction/backtesting/reports.py"
      via: "accepted/rejected parquet persistence and summary-file pattern reuse"
      pattern: "accepted|rejected|summary"
---

<objective>
Create the fourth Phase 06 slice: a read-only shadow/live monitor that reuses trusted artifact interfaces, scores only matched markets, and emits ranked accepted plus rejected outputs.

Purpose: Close D-15 through D-18 and make the Phase 06 goal operator-visible without adding any trade execution surface.
Output: Monitoring orchestration, ranked report outputs, a CLI command for shadow mode plus optional fresh collection, and failing-then-passing tests for scan orchestration and ranking behavior.
</objective>

## Phase Goal

**As a** operator, **I want to** run a read-only Kalshi EV scan that reuses trusted model artifacts and current snapshot evidence, **so that** I can rank executable opportunities and inspect every rejected or excluded market.

<execution_context>
@/Users/andrewlay/.codex/gsd-core/workflows/execute-plan.md
@/Users/andrewlay/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-CONTEXT.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-RESEARCH.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-PATTERNS.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-VALIDATION.md
@.planning/phases/05-kalshi-read-only-market-integration/05-03-SUMMARY.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-02-SUMMARY.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-03-SUMMARY.md
@src/tennisprediction/backtesting/replay.py
@src/tennisprediction/backtesting/reports.py
@src/tennisprediction/modeling/registry.py
@src/tennisprediction/modeling/datasets.py
@src/tennisprediction/kalshi/jobs.py
@src/tennisprediction/cli.py
</context>

## Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Read-only live or shadow scan with ranked executable opportunities | 06-04 | COVERED | This slice delivers the operator-visible Phase 06 outcome. |
| REQ | MKT-07 | Reuse the same feature and model interfaces used in training and backtesting | 06-04 | COVERED | Scan path reuses trusted artifact loading and replay-style prediction helpers. |
| REQ | MKT-08 | Rank opportunities by EV, edge, liquidity, confidence, and thresholds | 06-04 | COVERED | Monitor reports sort accepted rows and persist rejected/excluded counts. |
| RESEARCH | R-06-04-A | Shadow-first default with optional fresh collection only when credentials exist | 06-04 | COVERED | CLI defaults to shadow mode and calls `collect_kalshi_snapshots()` only on opt-in. |
| RESEARCH | R-06-04-B | `uv` is missing on PATH and ML/runtime packages are incomplete in `.venv` | 06-04 | COVERED | Verification uses `./.venv/bin/python -m pytest`; plan notes the `ml`-group prerequisite for real replay-backed runs. |
| CONTEXT | D-15 | Default flow is shadow-first and read-only; live path may collect fresh snapshots but still not execute trades | 06-04 | COVERED | The CLI exposes only read-only collection and scan orchestration. |
| CONTEXT | D-16 | Missing predictions, stale orderbooks, and unresolved mappings must emit explicit rejected/unscorable records | 06-04 | COVERED | Scan orchestration persists accepted plus rejected outputs instead of skipping failures. |
| CONTEXT | D-17 | Rank by EV, edge, liquidity, confidence, freshness and include required operator-facing columns | 06-04 | COVERED | Ranking, Rich output, and mapping-state/confidence fields are locked by tests. |
| CONTEXT | D-18 | Use deterministic mapping confidence tiers instead of hidden heuristics | 06-04 | COVERED | Monitor outputs surface the tier emitted by the resolver. |
| CONTEXT | D-19 | Persist both accepted and rejected scan outputs and show rejected/excluded summary counts | 06-04 | COVERED | Reports write both record sets plus summary files. |

## Artifacts this phase produces

- Files: `src/tennisprediction/backtesting/replay.py`, `src/tennisprediction/monitoring/__init__.py`, `src/tennisprediction/monitoring/scan.py`, `src/tennisprediction/monitoring/reports.py`, `src/tennisprediction/cli.py`, `tests/unit/test_live_scan_orchestration.py`, `tests/unit/test_live_monitor_reports.py`
- Functions: `predict_matches_for_canonical_ids`, `run_kalshi_ev_scan`, `write_live_monitor_reports`
- CLI surface: `scan-kalshi-ev`
- Output contract: `reports/monitoring/<run_id>/summary.json`, `accepted_opportunities.parquet`, `rejected_opportunities.parquet`, `ranked_opportunities.csv`, plus Rich table output for accepted rows and rejected/excluded counts, all carrying `mapping_state` and `mapping_confidence`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing orchestration and ranking tests for the read-only monitor</name>
  <read_first>
.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-CONTEXT.md
.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-VALIDATION.md
src/tennisprediction/backtesting/replay.py
src/tennisprediction/kalshi/jobs.py
src/tennisprediction/backtesting/reports.py
tests/unit/test_cli_smoke.py
  </read_first>
  <files>tests/unit/test_live_scan_orchestration.py, tests/unit/test_live_monitor_reports.py</files>
  <behavior>
    - Test 1: `run_kalshi_ev_scan()` in shadow mode loads the latest persisted snapshot tables, resolves mappings, derives executable inputs, reuses trusted prediction helpers, and emits accepted plus rejected outputs without any order-placement path.
    - Test 2: when `collect_fresh=True` and credentials are supplied, orchestration calls the existing Phase 05 read-only snapshot job before scoring and still never exposes execution surfaces.
    - Test 3: accepted rows are ranked by expected value, then edge, executable liquidity, confidence, and freshness per D-17.
    - Test 4: report outputs persist both accepted and rejected records and include `mapping_state`, `mapping_confidence`, and summary counts for `matched`, `ambiguous`, `unmatched`, `excluded`, and rejection reasons per D-19.
  </behavior>
  <action>Create failing orchestration and reporting tests with monkeypatched seams for `resolve_kalshi_market_mappings()`, `derive_executable_market_input()`, `predict_matches_for_canonical_ids()`, `evaluate_opportunities()`, and `collect_kalshi_snapshots()`. Keep the unit coverage independent from the currently incomplete ML runtime by stubbing trusted artifact outputs rather than depending on real `pandas`, `scikit-learn`, or `xgboost` availability. Lock the exact ranked columns required by D-17 and the persisted filenames required by this slice.</action>
  <verify>
    <automated>! ./.venv/bin/python -m pytest -q tests/unit/test_live_scan_orchestration.py tests/unit/test_live_monitor_reports.py -x</automated>
  </verify>
  <acceptance_criteria>
    - The tests fail before implementation and lock the shadow-first, read-only orchestration contract.
    - Ranking order and persisted report filenames are explicit enough that the executor does not need to infer them later.
    - Unit coverage does not assume the missing `ml` dependency group is already installed in `.venv`.
  </acceptance_criteria>
  <done>The slice has failing tests that lock monitor orchestration, ranking order, and persisted accepted/rejected outputs before implementation begins.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Restore the ML runtime prerequisite before replay-backed monitor delivery continues</name>
  <read_first>
.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-RESEARCH.md
.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-VALIDATION.md
pyproject.toml
uv.lock
src/tennisprediction/modeling/registry.py
src/tennisprediction/backtesting/replay.py
  </read_first>
  <files>.venv, pyproject.toml, uv.lock</files>
  <action>Before claiming MKT-07 is delivered, restore the missing replay runtime documented in `06-RESEARCH.md` using the repo-standard dependency source of truth. First verify whether `pyproject.toml` / `uv.lock` already describe the required `ml` group correctly; do not edit dependency specs unless they are actually incomplete or inconsistent. Then restore the environment by syncing the existing `ml` dependency group into `.venv` using the repo-standard `uv` workflow. If `uv` is unavailable on PATH or the sync cannot be completed in this session, stop here and hand control back to the operator rather than continuing with a degraded runtime. After the environment is restored, the executor may resume Task 3 and must include one real artifact-backed replay smoke check before Phase 06 is marked complete.</action>
  <verify>
    <manual>Confirm the repo-standard ML runtime is restored in `.venv` and that the environment is ready for a real replay-backed verification run before resuming Task 3.</manual>
  </verify>
  <what-built>Phase 06 now has an explicit blocking prerequisite: the trusted replay runtime must exist before live monitor delivery continues.</what-built>
  <how-to-verify>Ensure the repo-standard `ml` dependency group has been restored into `.venv` with the project’s normal `uv` workflow, and confirm the environment is ready to run `./.venv/bin/python -m pytest -q tests/unit/test_backtesting_replay.py -x` during the next task.</how-to-verify>
  <resume-signal>Type "approved" after the ML runtime is restored, or describe the blocker that still prevents replay-backed verification.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement trusted prediction reuse, monitor reports, and the read-only scan CLI</name>
  <read_first>
tests/unit/test_live_scan_orchestration.py
tests/unit/test_live_monitor_reports.py
tests/unit/test_backtesting_replay.py
src/tennisprediction/backtesting/replay.py
src/tennisprediction/modeling/registry.py
src/tennisprediction/modeling/datasets.py
src/tennisprediction/kalshi/jobs.py
src/tennisprediction/cli.py
.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-PATTERNS.md
  </read_first>
  <files>src/tennisprediction/backtesting/replay.py, src/tennisprediction/monitoring/__init__.py, src/tennisprediction/monitoring/scan.py, src/tennisprediction/monitoring/reports.py, src/tennisprediction/cli.py</files>
  <action>Extract a trusted prediction helper `predict_matches_for_canonical_ids()` from `src/tennisprediction/backtesting/replay.py` so the live monitor can reuse the same artifact manifest loading, feature-column ordering, and calibrated probability generation used in backtesting. Follow the `06-PATTERNS.md` analogs explicitly: shape `monitoring/scan.py` after the typed orchestration/result-builder style mapped from `src/tennisprediction/kalshi/jobs.py` and `src/tennisprediction/ev/opportunity.py`, shape `monitoring/reports.py` after the persisted accepted/rejected report layout mapped from `src/tennisprediction/backtesting/reports.py`, and extend `src/tennisprediction/cli.py` using the same single-Typer-app option and callback conventions mapped from the existing CLI entrypoint. Implement `run_kalshi_ev_scan()` in `src/tennisprediction/monitoring/scan.py` so the default path reads the latest persisted Kalshi snapshot batch, resolves mappings, rejects everything outside `matched`, carries forward both `mapping_state` and deterministic `mapping_confidence`, reuses the explicit side-orientation contract from `market_mapping_evidence` (`yes_canonical_player_id`, `no_canonical_player_id`, `yes_maps_to_player_a`, `no_maps_to_player_b`), derives executable market inputs, builds predictions for the matched `canonical_match_id` set, and feeds those into the upgraded EV engine. Add `write_live_monitor_reports()` in `src/tennisprediction/monitoring/reports.py` to persist `summary.json`, `accepted_opportunities.parquet`, `rejected_opportunities.parquet`, and `ranked_opportunities.csv` under `reports/monitoring/<run_id>`, plus a Rich table for accepted rows and summary counts for rejected and excluded rows. Extend `src/tennisprediction/cli.py` with `scan-kalshi-ev`, default it to shadow mode, add an opt-in `--collect-fresh` path that reuses the existing Phase 05 read-only snapshot job when credentials are supplied, and keep the command strictly non-execution. Because Task 2 restores the real replay runtime, this task should verify against actual trusted-artifact seams rather than only monkeypatched stubs.</action>
  <verify>
    <automated>./.venv/bin/python -m pytest -q tests/unit/test_backtesting_replay.py tests/unit/test_live_scan_orchestration.py tests/unit/test_live_monitor_reports.py tests/unit/test_cli_smoke.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Shadow mode can score the latest persisted batch without collecting fresh snapshots or exposing any execution surface.
    - Optional fresh collection reuses the existing read-only job and still flows through the same mapping, executable-pricing, and EV audit surfaces.
    - Accepted outputs are ranked per D-17, carry `mapping_state`, `mapping_confidence`, and the persisted YES/NO orientation contract, and rejected/excluded outputs remain persisted and visible per D-19.
    - The task verifies against a real artifact-backed replay path after the ML runtime prerequisite is restored by Task 2.
  </acceptance_criteria>
  <done>Phase 06 delivers a read-only shadow/live monitor that reuses trusted model interfaces, scores only matched markets, and persists ranked accepted plus rejected outputs for audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted snapshots or fresh collection→scan orchestrator | The monitor must stay read-only and treat missing or stale inputs as rejected records |
| trusted artifact bundle→prediction helper | Live scoring must reuse the existing artifact and feature contracts instead of inventing a second model-serving seam |
| decision batch→operator reports | Accepted and rejected records must both persist so auditability is preserved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | `src/tennisprediction/backtesting/replay.py` and `src/tennisprediction/monitoring/scan.py` | mitigate | Reuse the trusted artifact bundle loader and shared feature-column ordering via `predict_matches_for_canonical_ids()` instead of duplicating model-serving logic. |
| T-06-04-02 | Repudiation | `src/tennisprediction/monitoring/reports.py` | mitigate | Persist both accepted and rejected record sets plus summary counts for mapping states and rejection reasons. |
| T-06-04-03 | Elevation of Privilege | `src/tennisprediction/cli.py` | mitigate | Keep `scan-kalshi-ev` strictly read-only, with optional fresh collection wired only to the existing Phase 05 snapshot job and no order-placement primitives. |
</threat_model>

<verification>
- `./.venv/bin/python -m pytest -q tests/unit/test_live_scan_orchestration.py tests/unit/test_live_monitor_reports.py -x`
- `./.venv/bin/python -m pytest -q tests/unit/test_live_scan_orchestration.py tests/unit/test_live_monitor_reports.py tests/unit/test_cli_smoke.py -x`
- `./.venv/bin/python -m ruff check src/tennisprediction/monitoring src/tennisprediction/cli.py tests/unit/test_live_scan_orchestration.py tests/unit/test_live_monitor_reports.py`
- Full phase gate prerequisite: if `.venv` still lacks `joblib`, `pandas`, `scikit-learn`, or `xgboost`, install the `ml` dependency group before running `./.venv/bin/python -m pytest -q` or any replay-backed artifact verification that exercises real model loads.
</verification>

<success_criteria>
- Operator can run a read-only shadow scan from existing Kalshi snapshots and trusted model artifacts.
- Optional fresh collection remains read-only and feeds the same scan pipeline instead of a separate execution path.
- Ranked accepted opportunities and persisted rejected/excluded outputs expose enough evidence for later alerting and operational hardening.
</success_criteria>

<output>
Create `.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-04-SUMMARY.md` when done
</output>
