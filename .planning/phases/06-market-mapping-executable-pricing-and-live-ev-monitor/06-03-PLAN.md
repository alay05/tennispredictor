---
phase: 06-market-mapping-executable-pricing-and-live-ev-monitor
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - src/tennisprediction/backtesting/schemas.py
  - src/tennisprediction/ev/pricing.py
  - src/tennisprediction/ev/opportunity.py
  - src/tennisprediction/kalshi/executable.py
  - tests/unit/test_kalshi_executable_pricing.py
  - tests/unit/test_live_scan_pricing_contract.py
autonomous: true
requirements:
  - MKT-05
  - MKT-06
must_haves:
  truths:
    - "System derives buy-YES and buy-NO executable entry prices from side-specific orderbook evidence instead of the old complement assumption per D-09 and D-10."
    - "Executable liquidity and freshness are explicit fields with source labels and unscorable reason codes per D-11 and D-12."
    - "Accepted and rejected EV records preserve price source, liquidity source, freshness age, and fee/slippage assumptions per D-13 and D-14."
  artifacts:
    - path: "src/tennisprediction/kalshi/executable.py"
      provides: "Bid-only orderbook to side-specific executable pricing adapter"
      exports: ["derive_executable_market_input"]
    - path: "src/tennisprediction/backtesting/schemas.py"
      provides: "Phase 06 executable-side input contracts"
      exports: ["ExecutableSideInput", "ExecutableMarketInput"]
    - path: "src/tennisprediction/ev/opportunity.py"
      provides: "Accepted/rejected EV audit surface upgraded to carry executable price evidence"
      exports: ["evaluate_opportunities", "market_input_threshold_snapshot"]
    - path: "tests/unit/test_kalshi_executable_pricing.py"
      provides: "Regression coverage for bid-only orderbook to executable-side price derivation"
    - path: "tests/unit/test_live_scan_pricing_contract.py"
      provides: "Regression coverage for executable EV evidence fields and rejection behavior"
  key_links:
    - from: "src/tennisprediction/kalshi/executable.py"
      to: "src/tennisprediction/kalshi/snapshots.py"
      via: "converts persisted orderbook snapshot rows into executable-side inputs"
      pattern: "yes_levels_json|no_levels_json|collected_at_utc"
    - from: "src/tennisprediction/ev/opportunity.py"
      to: "src/tennisprediction/backtesting/schemas.py"
      via: "upgraded executable market-input contract replaces the old normalized positive-side scalar"
      pattern: "ExecutableMarketInput|ExecutableSideInput"
    - from: "src/tennisprediction/ev/pricing.py"
      to: "src/tennisprediction/ev/opportunity.py"
      via: "side evaluation now consumes explicit yes and no entry prices"
      pattern: "positive|negative|entry_price|freshness"
---

<objective>
Create the third Phase 06 slice: executable price, liquidity, freshness, and EV-input contracts that preserve the Phase 04 accepted/rejected audit surface while replacing the old complement assumption.

Purpose: Close D-09 through D-14 so live scoring uses executable-side Kalshi price evidence rather than a synthetic `1 - positive_side_price` shortcut.
Output: A new Kalshi executable-pricing adapter, upgraded EV input schemas, and failing-then-passing tests that preserve explicit price, liquidity, freshness, fee, and slippage evidence on every accepted or rejected record.
</objective>

## Phase Goal

**As a** operator, **I want to** convert fresh Kalshi orderbook snapshots into executable-side EV inputs, **so that** the scorer uses real buy-side evidence instead of a complement assumption.

<execution_context>
@/Users/andrewlay/.codex/gsd-core/workflows/execute-plan.md
@/Users/andrewlay/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-CONTEXT.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-RESEARCH.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-PATTERNS.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-VALIDATION.md
@.planning/phases/04-backtesting-and-ev-decision-core/04-03-SUMMARY.md
@.planning/phases/05-kalshi-read-only-market-integration/05-03-SUMMARY.md
@src/tennisprediction/backtesting/schemas.py
@src/tennisprediction/ev/pricing.py
@src/tennisprediction/ev/opportunity.py
@src/tennisprediction/kalshi/snapshots.py
</context>

## Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Executable EV inputs from fresh price sources | 06-03 | COVERED | This slice owns the executable input contract. |
| REQ | MKT-05 | Convert Kalshi prices and orderbook depth into a recorded market probability source | 06-03 | COVERED | `derive_executable_market_input()` produces side-specific entry-price evidence. |
| REQ | MKT-06 | Calculate executable edge and EV with price, liquidity, freshness, fee, and slippage assumptions recorded | 06-03 | COVERED | Upgraded EV contract preserves evidence on every decision row. |
| RESEARCH | R-06-03-A | Kalshi orderbooks are bid-only and asks must be derived from the reciprocal ladder | 06-03 | COVERED | Adapter derives yes/no executable entries explicitly instead of complementing one side later. |
| RESEARCH | R-06-03-B | Top-of-book liquidity is the recommended MVP default | 06-03 | COVERED | This slice uses top-of-book executable notional and records the source label so later aggregation can extend it safely. |
| CONTEXT | D-09 | Side-specific executable inputs replace the old single-scalar contract | 06-03 | COVERED | New executable-side DTOs in `backtesting/schemas.py`. |
| CONTEXT | D-10 | Midpoint and bids are diagnostics only; EV uses executable asks and size | 06-03 | COVERED | Unscorable sides stay rejected when there is no executable ask or size. |
| CONTEXT | D-11 | Liquidity means executable notional at the chosen price source | 06-03 | COVERED | Top-of-book notional is explicitly labeled. |
| CONTEXT | D-12 | Freshness comes from snapshot timestamp and stale snapshots are unscorable after 120 seconds | 06-03 | COVERED | Adapter emits freshness age and `stale_orderbook` rejection. |
| CONTEXT | D-13 | Preserve the Phase 04 accepted/rejected audit pattern | 06-03 | COVERED | `market_input_threshold_snapshot()` is extended, not replaced. |
| CONTEXT | D-14 | Fee and slippage remain explicit contract fields | 06-03 | COVERED | Threshold snapshot continues to expose both assumptions. |

## Artifacts this phase produces

- Files: `src/tennisprediction/kalshi/executable.py`, `src/tennisprediction/backtesting/schemas.py`, `src/tennisprediction/ev/pricing.py`, `src/tennisprediction/ev/opportunity.py`, `tests/unit/test_kalshi_executable_pricing.py`, `tests/unit/test_live_scan_pricing_contract.py`
- Dataclasses / models: `ExecutableSideInput`, `ExecutableMarketInput`
- Functions: `derive_executable_market_input`, `evaluate_opportunities`, `market_input_threshold_snapshot`
- Explicit Phase 06 stance: executable liquidity uses top-of-book notional in the MVP, and the negative side is evaluated against its own explicit entry price rather than `1 - positive_side_price`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing executable-pricing and EV-evidence contract tests</name>
  <read_first>
.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-CONTEXT.md
.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-VALIDATION.md
src/tennisprediction/backtesting/schemas.py
src/tennisprediction/ev/pricing.py
src/tennisprediction/ev/opportunity.py
tests/unit/test_backtesting_decisions.py
  </read_first>
  <files>tests/unit/test_kalshi_executable_pricing.py, tests/unit/test_live_scan_pricing_contract.py</files>
  <behavior>
    - Test 1: `derive_executable_market_input()` derives the buy-YES entry from the reciprocal no-bid ladder and the buy-NO entry from the reciprocal yes-bid ladder, never by complementing one side after the fact.
    - Test 2: missing opposing bid depth, zero executable size, or snapshots older than 120 seconds make that side unscorable with explicit reason codes such as `missing_executable_price`, `missing_executable_liquidity`, or `stale_orderbook`.
    - Test 3: `evaluate_opportunities()` compares the positive side to the explicit yes entry price and the negative side to the explicit no entry price, preserving `entry_price`, `entry_price_source`, `liquidity_source`, `freshness_age_seconds`, `fee_per_contract`, and `slippage_per_contract` in the decision snapshot.
    - Test 4: the old Phase 04 complement assumption no longer appears in live pricing tests for the negative side.
  </behavior>
  <action>Create failing tests around persisted `KalshiOrderbookSnapshotRow` fixtures plus one replay-style prediction row. Lock the exact evidence fields that must survive into accepted and rejected records: `selected_entry_price`, `entry_price_source`, `available_liquidity_dollars`, `liquidity_source`, `freshness_age_seconds`, `freshness_source`, `fee_per_contract`, `slippage_per_contract`, and the rejection codes for stale or unexecutable sides. Make the MVP top-of-book choice explicit in the tests so later multi-level aggregation can extend the contract without changing its meaning.</action>
  <verify>
    <automated>! ./.venv/bin/python -m pytest -q tests/unit/test_kalshi_executable_pricing.py tests/unit/test_live_scan_pricing_contract.py -x</automated>
  </verify>
  <acceptance_criteria>
    - The tests fail before implementation and encode the D-09 through D-14 rules.
    - The negative side is no longer priced by complementing the positive side after EV evaluation starts.
    - The explicit evidence fields needed by the later monitor are locked in the tests.
  </acceptance_criteria>
  <done>The slice has failing tests that lock executable-side price, liquidity, freshness, and EV-evidence semantics before the adapter exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the executable pricing adapter and side-specific EV contracts</name>
  <read_first>
tests/unit/test_kalshi_executable_pricing.py
tests/unit/test_live_scan_pricing_contract.py
src/tennisprediction/backtesting/schemas.py
src/tennisprediction/ev/pricing.py
src/tennisprediction/ev/opportunity.py
src/tennisprediction/kalshi/snapshots.py
.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-PATTERNS.md
  </read_first>
  <files>src/tennisprediction/backtesting/schemas.py, src/tennisprediction/ev/pricing.py, src/tennisprediction/ev/opportunity.py, src/tennisprediction/kalshi/executable.py</files>
  <action>Introduce `ExecutableSideInput` and `ExecutableMarketInput` in `src/tennisprediction/backtesting/schemas.py`, then implement `derive_executable_market_input()` in `src/tennisprediction/kalshi/executable.py` so persisted bid-only orderbook snapshots produce explicit yes and no buy-side entries with top-of-book `entry_price`, `entry_price_source`, `available_liquidity_dollars`, `liquidity_source`, `freshness_age_seconds`, and `freshness_source`. Follow the `06-PATTERNS.md` analogs explicitly: keep `kalshi/executable.py` aligned first to the evidence-rich accepted/rejected result-contract style mapped from `src/tennisprediction/ev/opportunity.py`, use `src/tennisprediction/ev/pricing.py` only as the secondary pure-helper analog for side math, and keep the upgraded contracts in `backtesting/schemas.py` aligned to the frozen dataclass pattern already used there. This slice must consume the explicit side-orientation fields persisted by 06-02 (`yes_canonical_player_id`, `no_canonical_player_id`, `yes_maps_to_player_a`, `no_maps_to_player_b`) so executable pricing and downstream EV logic never infer which canonical player each Kalshi side represents from title text alone. Upgrade `src/tennisprediction/ev/pricing.py` and `src/tennisprediction/ev/opportunity.py` to evaluate the negative side against its own explicit `no_entry_price` instead of `1 - positive_side_price`, preserve D-13’s accepted/rejected symmetry, and extend `market_input_threshold_snapshot()` with the executable pricing evidence plus D-14’s fee and slippage fields. Keep transport parsing out of the EV layer: this slice consumes persisted snapshot rows and typed executable inputs only.</action>
  <verify>
    <automated>./.venv/bin/python -m pytest -q tests/unit/test_backtesting_decisions.py tests/unit/test_kalshi_executable_pricing.py tests/unit/test_live_scan_pricing_contract.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Both sides have explicit executable entry-price contracts, source labels, and unscorable reasons.
    - The EV engine preserves the accepted/rejected audit surface while using explicit yes and no entry prices.
    - Stale or unexecutable sides stay rejected with inspectable evidence rather than silently disappearing.
  </acceptance_criteria>
  <done>Phase 06 has a side-specific executable pricing adapter and EV contract that records price, liquidity, freshness, and fee/slippage evidence on every decision row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bid-only orderbook rows→pricing adapter | Persisted Kalshi snapshots must be converted into executable-side prices without hidden assumptions |
| executable inputs→EV engine | Missing price, liquidity, or freshness evidence must fail closed before EV math accepts a market |
| EV engine→later monitor | Accepted and rejected rows must preserve the exact pricing evidence used for the decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | `src/tennisprediction/kalshi/executable.py` | mitigate | Derive explicit yes and no buy-side entries from the reciprocal opposing bid ladder and record the source label on each side. |
| T-06-03-02 | Repudiation | `src/tennisprediction/ev/opportunity.py` | mitigate | Preserve entry price, price source, liquidity source, freshness age, fee, and slippage in the threshold snapshot for both accepted and rejected records. |
| T-06-03-03 | Denial of Service | stale or thin orderbook inputs | mitigate | Reject stale snapshots, missing executable price, and missing executable liquidity with explicit reason codes before EV acceptance. |
</threat_model>

<verification>
- `./.venv/bin/python -m pytest -q tests/unit/test_kalshi_executable_pricing.py tests/unit/test_live_scan_pricing_contract.py -x`
- `./.venv/bin/python -m pytest -q tests/unit/test_backtesting_decisions.py tests/unit/test_kalshi_executable_pricing.py tests/unit/test_live_scan_pricing_contract.py -x`
- `./.venv/bin/python -m ruff check src/tennisprediction/kalshi/executable.py src/tennisprediction/ev tests/unit/test_kalshi_executable_pricing.py tests/unit/test_live_scan_pricing_contract.py`
</verification>

<success_criteria>
- Live pricing consumes explicit executable yes/no entry evidence instead of a complement shortcut.
- Liquidity and freshness are first-class rejection and audit fields.
- The Phase 04 opportunity record shape stays canonical while carrying the new executable evidence fields required for live monitoring.
</success_criteria>

<output>
Create `.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-03-SUMMARY.md` when done
</output>
