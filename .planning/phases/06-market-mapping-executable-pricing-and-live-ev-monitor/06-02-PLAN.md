---
phase: 06-market-mapping-executable-pricing-and-live-ev-monitor
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/tennisprediction/market_mapping/__init__.py
  - src/tennisprediction/market_mapping/schemas.py
  - src/tennisprediction/market_mapping/resolver.py
  - tests/unit/test_market_mapping_resolver.py
  - tests/unit/test_live_scan_rejections.py
autonomous: true
requirements:
  - MKT-03
  - MKT-04
must_haves:
  truths:
    - "Operator can classify every eligible Kalshi ATP market as `matched`, `ambiguous`, `unmatched`, or `excluded` per D-07."
    - "A market only reaches `matched` when player identity, player pairing, same-date timing evidence, and side-label semantics all agree per D-06."
    - "Every mapping attempt carries a deterministic `mapping_confidence` tier such as `exact_names`, `alias_override`, or `manual_review_required`, and only non-review-required matched rows may proceed to scoring per D-17 and D-18."
    - "Every matched evidence row persists explicit YES/NO orientation fields so later pricing and monitor slices can prove which canonical player each Kalshi side represents."
    - "Anything not in `matched` is persisted with evidence and explicit rejection reasons and is unavailable to the scorer per D-08."
  artifacts:
    - path: "src/tennisprediction/market_mapping/resolver.py"
      provides: "Market-to-match resolution, evidence persistence, and fail-closed scoring eligibility helpers"
      exports: ["resolve_kalshi_market_mappings", "persist_market_mapping_evidence", "require_matched_mapping"]
    - path: "src/tennisprediction/market_mapping/schemas.py"
      provides: "Mapping-state, deterministic confidence-tier, and evidence DTO contracts"
      exports: ["MarketMappingState", "MappingConfidenceTier", "MarketMappingEvidenceRow", "UnscorableMappingRecord"]
    - path: "tests/unit/test_market_mapping_resolver.py"
      provides: "Regression coverage for matched/ambiguous/unmatched/excluded resolution states"
    - path: "tests/unit/test_live_scan_rejections.py"
      provides: "Regression coverage that unresolved mappings cannot reach the scoring path"
  key_links:
    - from: "src/tennisprediction/market_mapping/resolver.py"
      to: "src/tennisprediction/market_mapping/normalization.py"
      via: "deterministic normalized-name identity evidence"
      pattern: "normalize_market_player_name|lookup_player_alias_override"
    - from: "src/tennisprediction/market_mapping/resolver.py"
      to: "src/tennisprediction/kalshi/storage.py"
      via: "loads the latest market and market-detail snapshot rows for mapping attempts"
      pattern: "kalshi_market_snapshots|kalshi_market_detail_snapshots"
    - from: "src/tennisprediction/market_mapping/resolver.py"
      to: "canonical_matches"
      via: "joins resolved player pairs onto canonical ATP match evidence"
      pattern: "canonical_match_id|tourney_date|round_name"
---

<objective>
Create the second Phase 06 slice: fail-closed Kalshi market-to-ATP match resolution with persisted evidence and explicit scoring rejection states.

Purpose: Close D-05 through D-08 before executable pricing or EV scoring is allowed to touch a market.
Output: Typed mapping-state contracts, a resolver that persists every mapping attempt into DuckDB evidence rows, and failing-then-passing tests for matched, ambiguous, unmatched, excluded, and unscorable outcomes.
</objective>

## Phase Goal

**As a** operator, **I want to** resolve Kalshi ATP markets into explicit mapping states, **so that** ambiguous or out-of-scope contracts are rejected before any executable EV logic runs.

<execution_context>
@/Users/andrewlay/.codex/gsd-core/workflows/execute-plan.md
@/Users/andrewlay/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-CONTEXT.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-RESEARCH.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-PATTERNS.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-VALIDATION.md
@.planning/phases/05-kalshi-read-only-market-integration/05-03-SUMMARY.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-01-SUMMARY.md
@src/tennisprediction/market_mapping/normalization.py
@src/tennisprediction/market_mapping/aliases.py
@src/tennisprediction/kalshi/storage.py
@src/tennisprediction/kalshi/snapshots.py
@src/tennisprediction/modeling/datasets.py
</context>

## Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Canonical match mapping states before scoring | 06-02 | COVERED | This slice owns the mapping-state boundary. |
| REQ | MKT-03 | Map Kalshi ATP markets to canonical ATP matches and player sides with explicit states | 06-02 | COVERED | Resolver plus persisted evidence rows. |
| REQ | MKT-04 | Refuse to score ambiguous or unmatched markets | 06-02 | COVERED | `require_matched_mapping()` and unscorable-record helpers lock the fail-closed rule. |
| RESEARCH | R-06-02-A | Require player-pair, title/subtitle semantics, and time-window evidence | 06-02 | COVERED | Resolver checks all three before `matched`. |
| RESEARCH | R-06-02-B | Start with same-date timing alignment and fail closed on misses | 06-02 | COVERED | This plan uses same-date `tourney_date` matching and reason code `timing_window_miss` instead of widening heuristics. |
| CONTEXT | D-05 | Eligible contracts are ATP head-to-head winner markets only | 06-02 | COVERED | Out-of-scope shapes become `excluded`. |
| CONTEXT | D-06 | `matched` requires one unique player pair, one canonical match, aligned timing, and consistent side semantics | 06-02 | COVERED | Encoded directly in resolver tests and evidence rows. |
| CONTEXT | D-07 | Mapping states must be `matched`, `ambiguous`, `unmatched`, or `excluded` | 06-02 | COVERED | Closed enum and persisted evidence row. |
| CONTEXT | D-08 | Persist raw strings, normalized strings, alias hits, candidate IDs, final state, and reason codes for every attempt | 06-02 | COVERED | DuckDB table `market_mapping_evidence` is part of the slice contract. |
| CONTEXT | D-17 / D-18 | Operator-facing output needs mapping state plus deterministic mapping confidence tier | 06-02 | COVERED | Resolver evidence rows carry the confidence tier for later monitor/report output. |

## Artifacts this phase produces

- Files: `src/tennisprediction/market_mapping/__init__.py`, `src/tennisprediction/market_mapping/schemas.py`, `src/tennisprediction/market_mapping/resolver.py`, `tests/unit/test_market_mapping_resolver.py`, `tests/unit/test_live_scan_rejections.py`
- Dataclasses / models: `MarketMappingState`, `MappingConfidenceTier`, `MarketMappingEvidenceRow`, `UnscorableMappingRecord`
- Functions: `resolve_kalshi_market_mappings`, `persist_market_mapping_evidence`, `require_matched_mapping`
- Persistence contract: new DuckDB table `market_mapping_evidence` with raw market strings, normalized player strings, alias-hit metadata, candidate canonical match IDs, explicit side-orientation fields (`yes_canonical_player_id`, `no_canonical_player_id`, `yes_maps_to_player_a`, `no_maps_to_player_b`), final state, and rejection reason codes

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing mapping-state and unresolved-market rejection tests</name>
  <read_first>
.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-CONTEXT.md
.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-VALIDATION.md
src/tennisprediction/market_mapping/normalization.py
src/tennisprediction/market_mapping/aliases.py
src/tennisprediction/kalshi/storage.py
tests/unit/test_kalshi_storage.py
  </read_first>
  <files>tests/unit/test_market_mapping_resolver.py, tests/unit/test_live_scan_rejections.py</files>
  <behavior>
    - Test 1: an ATP head-to-head winner market with uniquely resolved players, same-date timing, and consistent yes/no labels lands in `matched` with one `canonical_match_id`.
    - Test 2: futures, outrights, props, or non-ATP markets land in `excluded` with explicit reason codes per D-05.
    - Test 3: duplicate player identities, ambiguous side orientation, or multiple same-date canonical matches land in `ambiguous`; missing same-date evidence lands in `unmatched`.
    - Test 4: anything not in `matched` produces an explicit unscorable record and cannot be forwarded to the pricing or EV layers per D-08 and MKT-04.
    - Test 5: matched rows expose a deterministic `mapping_confidence` tier (`exact_names` or `alias_override`), while anything requiring human review is labeled `manual_review_required` and stays out of the accepted scoring path.
    - Test 6: matched rows persist side-orientation truth explicitly through `yes_canonical_player_id`, `no_canonical_player_id`, `yes_maps_to_player_a`, and `no_maps_to_player_b` so downstream pricing never has to infer which player a Kalshi side represents.
  </behavior>
  <action>Create failing resolver tests around a small DuckDB fixture containing `canonical_matches`, one latest Kalshi market snapshot row, and one latest market-detail snapshot row. Lock the exact evidence payload columns needed later by the monitor: `raw_title`, `raw_yes_sub_title`, `raw_no_sub_title`, `normalized_yes_player_name`, `normalized_no_player_name`, `alias_hit_player_ids_json`, `candidate_canonical_match_ids_json`, `mapping_state`, `mapping_confidence`, `canonical_match_id`, `yes_canonical_player_id`, `no_canonical_player_id`, `yes_maps_to_player_a`, `no_maps_to_player_b`, and `rejection_reason_codes_json`. Use same-date matching only in the fixtures so the plan enforces the narrow fail-closed timing rule from research instead of silently widening the date window.</action>
  <verify>
    <automated>! ./.venv/bin/python -m pytest -q tests/unit/test_market_mapping_resolver.py tests/unit/test_live_scan_rejections.py -x</automated>
  </verify>
  <acceptance_criteria>
    - The tests fail before implementation and encode the exact D-05 through D-08 rules.
    - Out-of-scope markets, unresolved names, and timing misses are separated into distinct states with distinct reason codes.
    - The future monitor can reuse the rejection helper without redefining mapping failure semantics.
  </acceptance_criteria>
  <done>The slice has failing tests that lock stateful mapping outcomes and refusal-to-score behavior before the resolver exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fail-closed mapping resolution and evidence persistence</name>
  <read_first>
tests/unit/test_market_mapping_resolver.py
tests/unit/test_live_scan_rejections.py
src/tennisprediction/market_mapping/normalization.py
src/tennisprediction/market_mapping/aliases.py
src/tennisprediction/kalshi/storage.py
src/tennisprediction/kalshi/snapshots.py
.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-PATTERNS.md
  </read_first>
  <files>src/tennisprediction/market_mapping/__init__.py, src/tennisprediction/market_mapping/schemas.py, src/tennisprediction/market_mapping/resolver.py</files>
  <action>Extend `src/tennisprediction/market_mapping/schemas.py` with `MarketMappingState`, `MappingConfidenceTier`, `MarketMappingEvidenceRow`, and `UnscorableMappingRecord`. Follow the `06-PATTERNS.md` analogs explicitly: shape `market_mapping/resolver.py` after the typed batch/result structure mapped to `src/tennisprediction/ev/opportunity.py`, shape evidence persistence after the stable DuckDB row-builder pattern mapped to `src/tennisprediction/kalshi/storage.py` and `src/tennisprediction/features/persistence.py`, and keep schema contracts aligned to the frozen dataclass style mapped from existing DTO modules. Implement `resolve_kalshi_market_mappings()` and `persist_market_mapping_evidence()` in `src/tennisprediction/market_mapping/resolver.py` so the resolver loads the latest market and market-detail snapshots, applies D-05 contract-shape filters, resolves both player names through the deterministic normalizer and additive alias artifact, assigns a deterministic `mapping_confidence` tier (`exact_names`, `alias_override`, or `manual_review_required`), and then checks D-06’s evidence chain in order: unique player identities, one canonical player pair, same-date `canonical_matches.tourney_date` alignment, and consistent winner-side title/subtitle semantics. Persist every attempt into a DuckDB table named `market_mapping_evidence`, including the explicit side-orientation contract fields `yes_canonical_player_id`, `no_canonical_player_id`, `yes_maps_to_player_a`, and `no_maps_to_player_b`, and expose `require_matched_mapping()` so later pricing/scan code can reuse the exact MKT-04 fail-closed rule while also refusing `manual_review_required` rows without inferring side orientation again. Do not call the EV engine, do not widen the timing window automatically, and do not invent fuzzy match acceptance paths in this plan.</action>
  <verify>
    <automated>./.venv/bin/python -m pytest -q tests/unit/test_market_mapping_resolver.py tests/unit/test_live_scan_rejections.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Every mapping attempt persists one evidence row with raw strings, normalized strings, alias hits, candidate match IDs, explicit YES/NO orientation fields, final state, and rejection reason codes.
    - `matched` only occurs when the full D-06 evidence chain succeeds and same-date timing matches one canonical ATP match.
    - Anything not in `matched` is reusable as an explicit unscorable input for the later monitor rather than being silently dropped.
  </acceptance_criteria>
  <done>Phase 06 has a deterministic, auditable market-to-match mapper that fails closed and persists evidence for every attempted resolution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kalshi snapshot rows→resolver | Market titles and subtitles are untrusted and must be filtered, normalized, and evidence-checked before matching |
| canonical match rows→mapping state | Duplicate or same-day rematches must not collapse into an arbitrary `matched` result |
| mapping evidence→later scoring | Only `matched` rows may cross into pricing or EV logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | `src/tennisprediction/market_mapping/resolver.py` | mitigate | Enforce D-05 filters and D-06 evidence checks in a fixed order before assigning `matched`. |
| T-06-02-02 | Repudiation | `market_mapping_evidence` persistence | mitigate | Persist raw strings, normalized strings, alias hits, candidate match IDs, final state, and reason codes for every mapping attempt. |
| T-06-02-03 | Elevation of Privilege | scoring eligibility | mitigate | Gate pricing with `require_matched_mapping()` so ambiguous, unmatched, and excluded rows cannot reach EV evaluation. |
</threat_model>

<verification>
- `./.venv/bin/python -m pytest -q tests/unit/test_market_mapping_resolver.py tests/unit/test_live_scan_rejections.py -x`
- `./.venv/bin/python -m pytest -q tests/unit/test_kalshi_storage.py tests/unit/test_market_mapping_resolver.py tests/unit/test_live_scan_rejections.py -x`
- `./.venv/bin/python -m ruff check src/tennisprediction/market_mapping tests/unit/test_market_mapping_resolver.py tests/unit/test_live_scan_rejections.py`
</verification>

<success_criteria>
- Operator can classify each latest Kalshi ATP market into `matched`, `ambiguous`, `unmatched`, or `excluded`.
- Matching requires exact evidence rather than heuristics hidden inside one “confidence” score.
- Markets outside `matched` are explicitly unscorable and available for audit rather than silently skipped.
</success_criteria>

<output>
Create `.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-02-SUMMARY.md` when done
</output>
