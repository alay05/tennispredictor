---
phase: 06-market-mapping-executable-pricing-and-live-ev-monitor
plan: 01
type: execute
wave: 1
depends_on:
  - 05-03
files_modified:
  - src/tennisprediction/market_mapping/__init__.py
  - src/tennisprediction/market_mapping/schemas.py
  - src/tennisprediction/market_mapping/normalization.py
  - src/tennisprediction/market_mapping/aliases.py
  - src/tennisprediction/market_mapping/player_alias_overrides.json
  - tests/unit/test_market_mapping_normalization.py
  - tests/unit/test_market_mapping_aliases.py
autonomous: true
requirements:
  - MKT-01
  - MKT-02
must_haves:
  truths:
    - "Operator can normalize Kalshi and canonical ATP player names into one deterministic comparison key per D-01."
    - "Manual alias overrides are additive, timestamped, and auditable per D-03 without mutating canonical player identities per D-04."
    - "Names that do not resolve uniquely stay unresolved instead of auto-accepting ambiguous candidates per D-02."
  artifacts:
    - path: "src/tennisprediction/market_mapping/normalization.py"
      provides: "Deterministic Phase 06 player-name normalization helpers"
      exports: ["normalize_market_player_name"]
    - path: "src/tennisprediction/market_mapping/aliases.py"
      provides: "Repo-local alias override loading and additive lookup helpers"
      exports: ["load_player_alias_overrides", "lookup_player_alias_override"]
    - path: "src/tennisprediction/market_mapping/schemas.py"
      provides: "Typed alias and name-resolution contracts"
      exports: ["PlayerAliasOverrideRecord", "PlayerNameResolutionResult"]
    - path: "src/tennisprediction/market_mapping/player_alias_overrides.json"
      provides: "Initial version-controlled alias artifact with explicit audit fields"
    - path: "tests/unit/test_market_mapping_normalization.py"
      provides: "Regression coverage for D-01 and D-02 normalization behavior"
    - path: "tests/unit/test_market_mapping_aliases.py"
      provides: "Regression coverage for D-03 additive alias override behavior"
  key_links:
    - from: "src/tennisprediction/market_mapping/normalization.py"
      to: "src/tennisprediction/domain/normalization.py"
      via: "same deterministic pure-function normalization style"
      pattern: "strip|normalize|deterministic"
    - from: "src/tennisprediction/market_mapping/aliases.py"
      to: "src/tennisprediction/market_mapping/player_alias_overrides.json"
      via: "repo-local JSON artifact with explicit audit metadata"
      pattern: "raw_market_name|normalized_market_name|canonical_player_id|created_at_utc|updated_at_utc"
    - from: "src/tennisprediction/market_mapping/aliases.py"
      to: "src/tennisprediction/config.py"
      via: "repo-local path safety for the version-controlled alias artifact"
      pattern: "_resolve_repo_path|repo_root"
---

<objective>
Create the first Phase 06 slice: deterministic player-name normalization plus auditable alias overrides for Kalshi-to-ATP identity matching.

Purpose: Lock D-01 through D-04 before any market-to-match resolver is allowed to infer player identity.
Output: A new `tennisprediction.market_mapping` package with typed alias contracts, deterministic normalizers, a version-controlled alias artifact, and failing-then-passing unit tests.
</objective>

## Phase Goal

**As a** operator, **I want to** normalize Kalshi and ATP player names and maintain auditable alias overrides, **so that** later market matching starts from deterministic player-identity evidence instead of fuzzy guesswork.

<execution_context>
@/Users/andrewlay/.codex/gsd-core/workflows/execute-plan.md
@/Users/andrewlay/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-CONTEXT.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-RESEARCH.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-PATTERNS.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-VALIDATION.md
@src/tennisprediction/config.py
@src/tennisprediction/domain/normalization.py
@tests/unit/test_normalization.py
</context>

## Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Deterministic player identity evidence before scoring | 06-01 | COVERED | This slice establishes the identity boundary the later mapper depends on. |
| REQ | MKT-01 | Deterministic candidate identities from Sackmann and Kalshi names | 06-01 | COVERED | `normalize_market_player_name()` and typed resolution results. |
| REQ | MKT-02 | Auditable manual player alias overrides | 06-01 | COVERED | Version-controlled JSON artifact plus schema-validated loader. |
| RESEARCH | R-06-01-A | Deterministic first, fuzzy only for review | 06-01 | COVERED | This MVP slice stays deterministic-only and does not add fuzzy auto-resolution logic. |
| RESEARCH | R-06-01-B | `RapidFuzz` is optional and `SUS` if installed | 06-01 | COVERED | No new dependency is added in this slice; ambiguity remains unresolved per D-02/D-04. |
| CONTEXT | D-01 | Normalize names via lowercase, ASCII fold, punctuation stripping, whitespace collapse, preserved token order | 06-01 | COVERED | Encoded directly in normalization tests and helper behavior. |
| CONTEXT | D-02 | Auto-resolution only on unique deterministic match or explicit alias override | 06-01 | COVERED | Ambiguous names stay unresolved. |
| CONTEXT | D-03 | Alias overrides must be additive and auditable | 06-01 | COVERED | Required JSON fields and timestamps are enforced. |
| CONTEXT | D-04 | No canonical player merges or identity rewrites | 06-01 | COVERED | Alias helpers map names to existing canonical IDs only. |

## Artifacts this phase produces

- Files: `src/tennisprediction/market_mapping/__init__.py`, `src/tennisprediction/market_mapping/schemas.py`, `src/tennisprediction/market_mapping/normalization.py`, `src/tennisprediction/market_mapping/aliases.py`, `src/tennisprediction/market_mapping/player_alias_overrides.json`, `tests/unit/test_market_mapping_normalization.py`, `tests/unit/test_market_mapping_aliases.py`
- Dataclasses / models: `PlayerAliasOverrideRecord`, `PlayerNameResolutionResult`
- Functions: `normalize_market_player_name`, `load_player_alias_overrides`, `lookup_player_alias_override`
- Explicit Phase 06 stance: this slice does not install `RapidFuzz` or any other fuzzy scorer; unresolved duplicates remain unresolved until an explicit alias override exists

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing normalization and alias-audit tests for D-01 through D-04</name>
  <read_first>
.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-CONTEXT.md
.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-VALIDATION.md
src/tennisprediction/config.py
src/tennisprediction/domain/normalization.py
tests/unit/test_normalization.py
  </read_first>
  <files>tests/unit/test_market_mapping_normalization.py, tests/unit/test_market_mapping_aliases.py</files>
  <behavior>
    - Test 1: `normalize_market_player_name("João   Fonseca!")` returns `joao fonseca`, collapses internal whitespace, strips punctuation, and preserves token order per D-01.
    - Test 2: two canonical players that normalize to the same key stay unresolved unless one explicit alias override exists per D-02 and D-04.
    - Test 3: alias rows require `raw_market_name`, `normalized_market_name`, `canonical_player_id`, `canonical_player_name`, `source_note`, `created_at_utc`, and `updated_at_utc` per D-03.
    - Test 4: alias lookups are additive only; malformed or duplicate rows fail loudly and do not rewrite canonical player IDs.
  </behavior>
  <action>Create failing tests with small canonical-player fixtures and one empty alias artifact fixture. Keep the slice deterministic: do not import or reference `RapidFuzz`, edit-distance scoring, or any fuzzy auto-accept path in the tests. Make the repository constraint explicit by asserting the alias artifact is read from a version-controlled repo path rather than `data/`, which is gitignored and would violate D-03’s auditability goal.</action>
  <verify>
    <automated>! ./.venv/bin/python -m pytest -q tests/unit/test_market_mapping_normalization.py tests/unit/test_market_mapping_aliases.py -x</automated>
  </verify>
  <acceptance_criteria>
    - The new tests fail before implementation and encode the exact D-01 through D-04 rules.
    - The tests make ambiguity fail closed rather than accepting a “best guess.”
    - The alias artifact contract is explicit enough that a later executor can implement it without reopening the context files.
  </acceptance_criteria>
  <done>The slice has failing tests that lock deterministic normalization and auditable additive alias behavior before any implementation is written.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic name normalization and additive alias override loading</name>
  <read_first>
tests/unit/test_market_mapping_normalization.py
tests/unit/test_market_mapping_aliases.py
src/tennisprediction/config.py
src/tennisprediction/domain/normalization.py
.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-PATTERNS.md
  </read_first>
  <files>src/tennisprediction/market_mapping/__init__.py, src/tennisprediction/market_mapping/schemas.py, src/tennisprediction/market_mapping/normalization.py, src/tennisprediction/market_mapping/aliases.py, src/tennisprediction/market_mapping/player_alias_overrides.json</files>
  <action>Implement `normalize_market_player_name()` in `src/tennisprediction/market_mapping/normalization.py` with D-01’s exact pipeline: lowercase, ASCII/diacritic folding, punctuation stripping, whitespace collapse, and preserved given-name/surname order. Follow the `06-PATTERNS.md` analogs explicitly: shape `market_mapping/normalization.py` after the deterministic pure-function style in `src/tennisprediction/domain/normalization.py`, shape `market_mapping/aliases.py` after the repo-path-safe helper pattern in `src/tennisprediction/config.py`, and keep the JSON artifact contract aligned to the audit-heavy dataclass style mapped for `market_mapping/schemas.py`. Add immutable alias and resolution contracts in `src/tennisprediction/market_mapping/schemas.py`, then implement `load_player_alias_overrides()` and `lookup_player_alias_override()` in `src/tennisprediction/market_mapping/aliases.py` around a version-controlled JSON artifact at `src/tennisprediction/market_mapping/player_alias_overrides.json`. Validate every row for the D-03 audit fields, reject duplicate raw or normalized keys, and never mutate canonical IDs or auto-resolve non-unique normalized names per D-02 and D-04. Keep this MVP slice deterministic-only: do not add `RapidFuzz`, edit-distance helpers, or canonical-player merge logic in this plan.</action>
  <verify>
    <automated>./.venv/bin/python -m pytest -q tests/unit/test_market_mapping_normalization.py tests/unit/test_market_mapping_aliases.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `normalize_market_player_name()` passes the D-01 examples and stays pure and deterministic.
    - The alias loader only accepts additive mappings onto existing canonical player IDs and rejects malformed or duplicate audit rows.
    - The initial alias artifact exists in a version-controlled repo path and can be reviewed in code review without digging through ignored runtime data.
  </acceptance_criteria>
  <done>Phase 06 has a deterministic player-name identity layer and an auditable alias artifact that later mapping code can consume safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw market name→normalizer | Untrusted text must normalize deterministically before any candidate lookup |
| alias artifact→resolver | Manual overrides must be schema-validated and additive before they influence identity resolution |
| canonical player IDs→later mapping | No slice in Phase 06 may rewrite Phase 01 canonical player identities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | `src/tennisprediction/market_mapping/player_alias_overrides.json` | mitigate | Validate required D-03 fields, reject duplicate keys, and keep the artifact in version control rather than ignored runtime storage. |
| T-06-01-02 | Spoofing | `src/tennisprediction/market_mapping/normalization.py` | mitigate | Normalize names deterministically and refuse non-unique matches instead of accepting fuzzy best guesses. |
| T-06-01-03 | Repudiation | `src/tennisprediction/market_mapping/aliases.py` | mitigate | Preserve `source_note`, `created_at_utc`, and `updated_at_utc` on every alias row so manual decisions remain reviewable. |
</threat_model>

<verification>
- `./.venv/bin/python -m pytest -q tests/unit/test_market_mapping_normalization.py tests/unit/test_market_mapping_aliases.py -x`
- `./.venv/bin/python -m pytest -q tests/unit/test_normalization.py tests/unit/test_market_mapping_normalization.py tests/unit/test_market_mapping_aliases.py -x`
- `./.venv/bin/python -m ruff check src/tennisprediction/market_mapping tests/unit/test_market_mapping_normalization.py tests/unit/test_market_mapping_aliases.py`
</verification>

<success_criteria>
- Operator-facing normalization rules are deterministic and test-locked.
- Alias overrides are additive, timestamped, and reviewable from a version-controlled artifact.
- Ambiguous normalized names remain unresolved until a later plan supplies enough evidence or an explicit alias override exists.
</success_criteria>

<output>
Create `.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-01-SUMMARY.md` when done
</output>
