---
phase: 07-alerts-and-operational-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/tennisprediction/config.py
  - src/tennisprediction/cli.py
  - src/tennisprediction/operations.py
  - tests/unit/test_cli_smoke.py
  - tests/unit/test_cli_commands.py
autonomous: true
requirements:
  - OPS-02
  - OPS-04
must_haves:
  truths:
    - "Operator can invoke the packaged `tennisprediction` entrypoint and see one-shot commands for ingestion, feature build, training, evaluation, backtesting, snapshot collection, scanning, and report review."
    - "Operational settings stay repo-local and cover thresholds, model artifact selection, report selection, storage paths, and terminal/file alert channels per D-01 and D-07."
    - "The CLI remains one-shot only and explicitly does not introduce continuous polling, daemonized services, or watch mode per D-06."
  artifacts:
    - path: "src/tennisprediction/operations.py"
      provides: "Project-owned one-shot orchestration helpers behind the CLI command tree"
      exports: ["ingest_snapshot", "build_features", "train_artifact_bundle", "evaluate_artifact", "run_backtest", "report_monitoring_run"]
    - path: "src/tennisprediction/cli.py"
      provides: "Packaged Typer command tree for the operator workflow"
      exports: ["app"]
    - path: "tests/unit/test_cli_commands.py"
      provides: "Regression coverage for the packaged entrypoint, command tree, and one-shot dispatch"
  key_links:
    - from: "pyproject.toml"
      to: "src/tennisprediction/cli.py"
      via: "the packaged script target must invoke the real Typer app instead of the callback function"
      pattern: "project.scripts|tennisprediction.cli"
    - from: "src/tennisprediction/cli.py"
      to: "src/tennisprediction/operations.py"
      via: "commands dispatch through project-owned orchestration helpers rather than inline stage logic"
      pattern: "ingest_snapshot|build_features|train_artifact_bundle|run_backtest"
    - from: "src/tennisprediction/config.py"
      to: "src/tennisprediction/operations.py"
      via: "repo-local settings provide default thresholds, model/report selections, paths, and terminal/file channel choices"
      pattern: "reports_dir|models_dir|duckdb_path|alert_channels|report_selection"
---

<objective>
Create the CLI and operational-configuration anchor slice for Phase 07 by fixing the packaged entrypoint first, then expanding the command tree through project-owned one-shot orchestration helpers.

Purpose: Deliver OPS-04 and the command/configuration portion of OPS-02 early enough that later report, logging, and docs work target the real operator path.
Output: A working packaged `tennisprediction` entrypoint, one-shot CLI commands across the pipeline, repo-local operational settings, and failing-then-passing command tests.
</objective>

## Phase Goal

**As an** operator, **I want to** run the ATP-to-Kalshi workflow from the packaged CLI without shell-only glue, **so that** the later report, logging, and documentation slices are grounded in the real operator path.

<execution_context>
@/Users/andrewlay/.codex/gsd-core/workflows/execute-plan.md
@/Users/andrewlay/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-alerts-and-operational-hardening/07-CONTEXT.md
@.planning/phases/07-alerts-and-operational-hardening/07-RESEARCH.md
@.planning/phases/07-alerts-and-operational-hardening/07-PATTERNS.md
@.planning/phases/07-alerts-and-operational-hardening/07-VALIDATION.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-04-SUMMARY.md
@.planning/phases/05-kalshi-read-only-market-integration/05-03-SUMMARY.md
@src/tennisprediction/cli.py
@src/tennisprediction/config.py
@src/tennisprediction/storage/duckdb.py
@src/tennisprediction/ingestion/validation.py
@src/tennisprediction/features/runner.py
@src/tennisprediction/features/persistence.py
@src/tennisprediction/modeling/datasets.py
@src/tennisprediction/modeling/splits.py
@src/tennisprediction/modeling/baselines.py
@src/tennisprediction/modeling/calibration.py
@src/tennisprediction/modeling/registry.py
@src/tennisprediction/backtesting/replay.py
@src/tennisprediction/backtesting/reports.py
@src/tennisprediction/monitoring/reports.py
</context>

## Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Operator can run the full workflow through audited CLI commands | 07-03 | COVERED | This is the Phase 07 anchor slice and must ship before later CLI/docs/report work. |
| REQ | OPS-02 | Configurable thresholds, model artifact selection, report selection, storage paths, and terminal/file alert channel settings for one-shot runs | 07-03 | COVERED | The CLI and settings surface implements the locked one-shot configuration contract directly, while continuous-run controls remain absent per D-06 and D-07. |
| REQ | OPS-04 | CLI commands for ingestion, feature build, training, evaluation, backtesting, collection, scanning, and reporting | 07-03 | COVERED | The packaged command tree owns the end-to-end operator path. |
| RESEARCH | R-07-03-A | The packaged CLI entrypoint is currently broken and must be fixed before new CLI/docs work | 07-03 | COVERED | `[project.scripts]` and CLI smoke tests are part of this slice. |
| RESEARCH | R-07-03-B | Use existing Typer + `pydantic-settings` seams and avoid new runtime dependencies | 07-03 | COVERED | The command tree expands the current app and repo-local settings rather than adding a second CLI/config layer. |
| CONTEXT | D-01 | Alert channels remain terminal/file-only | 07-03 | COVERED | Operational settings restrict channel selection to terminal/file and never introduce external delivery. |
| CONTEXT | D-06 | Commands are one-shot only | 07-03 | COVERED | This slice rejects watch-mode, continuous polling, and daemon semantics outright. |
| CONTEXT | D-07 | Settings cover thresholds, artifact/report selection, and storage paths while polling controls stay out of scope | 07-03 | COVERED | The plan implements the full one-shot settings contract directly and keeps continuous-run controls out of scope. |

## Artifacts this phase produces

- Files: `pyproject.toml`, `src/tennisprediction/config.py`, `src/tennisprediction/cli.py`, `src/tennisprediction/operations.py`, `tests/unit/test_cli_smoke.py`, `tests/unit/test_cli_commands.py`
- Commands: packaged `tennisprediction` help plus one-shot subcommands for ingest, features, training, evaluation, backtesting, snapshot collection, scanning, and reporting
- Settings contract: repo-local defaults for thresholds, model artifact selection, report selection, output paths, and terminal/file alert channels only

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing packaged-entrypoint and command-dispatch tests</name>
  <read_first>
.planning/phases/07-alerts-and-operational-hardening/07-CONTEXT.md
.planning/phases/07-alerts-and-operational-hardening/07-VALIDATION.md
pyproject.toml
src/tennisprediction/cli.py
src/tennisprediction/config.py
tests/unit/test_cli_smoke.py
  </read_first>
  <files>tests/unit/test_cli_smoke.py, tests/unit/test_cli_commands.py</files>
  <behavior>
    - Test 1: the packaged `tennisprediction` entrypoint shows CLI help instead of no-op behavior.
    - Test 2: the command tree exposes one-shot commands for ingestion, feature build, training, evaluation, backtesting, snapshot collection, scanning, and reporting.
    - Test 3: each command dispatches through a project-owned helper seam rather than embedding stage logic directly in CLI tests.
    - Test 4: watch-mode or polling-interval style flags are absent or rejected because D-06 and D-07 lock this phase to one-shot commands only.
  </behavior>
  <action>Create `tests/unit/test_cli_commands.py` and expand `tests/unit/test_cli_smoke.py` so they fail against the current CLI. Cover the packaged entrypoint, the expected command tree, and the dispatch seam into new orchestration helpers. Make the final OPS-02 contract explicit in the tests: thresholds, model artifact selection, report selection, storage paths, and terminal/file channels must be configurable for one-shot runs, while polling interval, continuous-run, and watch-mode behavior remain absent or rejected per D-06 and D-07.</action>
  <verify>
    <automated>! UV_CACHE_DIR=.uv-cache python3 -m uv run pytest -q tests/unit/test_cli_smoke.py tests/unit/test_cli_commands.py -x</automated>
  </verify>
  <acceptance_criteria>
    - The tests fail before implementation and prove the current packaged entrypoint and command coverage are insufficient.
    - Command presence and one-shot scope are asserted directly.
    - The CLI test contract closes the `tests/unit/test_cli_commands.py` Wave 0 gap from `07-VALIDATION.md`.
  </acceptance_criteria>
  <done>The CLI slice has failing tests that lock the packaged entrypoint, command tree, and one-shot scope before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix the packaged entrypoint and add one-shot orchestration helpers plus operational settings</name>
  <read_first>
tests/unit/test_cli_smoke.py
tests/unit/test_cli_commands.py
src/tennisprediction/cli.py
src/tennisprediction/config.py
src/tennisprediction/storage/duckdb.py
src/tennisprediction/ingestion/validation.py
src/tennisprediction/features/runner.py
src/tennisprediction/features/persistence.py
src/tennisprediction/modeling/datasets.py
src/tennisprediction/modeling/splits.py
src/tennisprediction/modeling/baselines.py
src/tennisprediction/modeling/calibration.py
src/tennisprediction/modeling/registry.py
src/tennisprediction/backtesting/replay.py
src/tennisprediction/backtesting/reports.py
src/tennisprediction/monitoring/reports.py
.planning/phases/07-alerts-and-operational-hardening/07-PATTERNS.md
  </read_first>
  <files>pyproject.toml, src/tennisprediction/config.py, src/tennisprediction/cli.py, src/tennisprediction/operations.py</files>
  <action>Fix the packaged console script first by pointing `[project.scripts]` at a real Typer entrypoint instead of the callback-only `main`, then keep one Typer app and route the full operator workflow through a new `src/tennisprediction/operations.py` seam. In `src/tennisprediction/config.py`, extend repo-local settings with defaults for thresholds, model artifact selection, report selection, output paths, and alert channels limited to `terminal` and `file` per D-01 and D-07. In `src/tennisprediction/operations.py`, compose the existing public stage functions into one-shot helpers for ingestion, feature build, training, evaluation, backtesting, snapshot collection, scan orchestration, and monitoring-report review. In `src/tennisprediction/cli.py`, expose those helpers as operator commands and explicitly reject or omit polling interval, watch, or daemon flags so the CLI stays inside the resolved D-06 and D-07 one-shot scope.</action>
  <verify>
    <automated>UV_CACHE_DIR=.uv-cache python3 -m uv run pytest -q tests/unit/test_cli_smoke.py tests/unit/test_cli_commands.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tennisprediction --help` works through the packaged entrypoint and exposes the end-to-end one-shot command tree.
    - Repo-local settings cover thresholds, model artifact selection, report selection, storage paths, and terminal/file channels without widening Phase 07 into continuous polling infrastructure.
    - Command handlers delegate into project-owned orchestration helpers so later logging, docs, and report work target one stable operator path.
  </acceptance_criteria>
  <done>Phase 07 now has a working packaged CLI entrypoint, one-shot operator commands across the workflow, and repo-local operational settings aligned with the locked scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packaged entrypoint→CLI dispatcher | The real operator path must invoke the Typer app and not silently no-op |
| CLI options→repo-local settings | Configurable paths and runtime options must remain inside the repository boundary |
| operator command request→pipeline orchestration | Commands must stay one-shot and read-only where the project scope requires it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Denial of Service | `pyproject.toml` packaged script target | mitigate | Add a failing packaged-entrypoint smoke test and fix `[project.scripts]` to invoke the real Typer app. |
| T-07-03-02 | Tampering | `src/tennisprediction/config.py` | mitigate | Preserve repo-local path validation for all new operational settings and keep alert channels constrained to terminal/file. |
| T-07-03-03 | Elevation of Privilege | `src/tennisprediction/cli.py` and `src/tennisprediction/operations.py` | mitigate | Omit or reject polling, watch, daemon, and execution-oriented options so the CLI stays one-shot-only per D-06. |
</threat_model>

<verification>
- `UV_CACHE_DIR=.uv-cache python3 -m uv run pytest -q tests/unit/test_cli_smoke.py tests/unit/test_cli_commands.py -x`
- `UV_CACHE_DIR=.uv-cache python3 -m uv run ruff check src/tennisprediction/cli.py src/tennisprediction/config.py src/tennisprediction/operations.py tests/unit/test_cli_smoke.py tests/unit/test_cli_commands.py`
</verification>

<success_criteria>
- The packaged `tennisprediction` command works and exposes the real operator path for the rest of Phase 07.
- Operators can invoke one-shot commands across ingestion, features, modeling, backtesting, collection, scanning, and report review.
- Configurable runtime knobs match the locked Phase 07 one-shot settings contract while continuous-run controls remain absent.
</success_criteria>

<output>
Create `.planning/phases/07-alerts-and-operational-hardening/07-03-SUMMARY.md` when done
</output>
