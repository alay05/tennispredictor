---
phase: 07-alerts-and-operational-hardening
plan: 01
type: execute
wave: 2
depends_on:
  - 07-03
files_modified:
  - src/tennisprediction/monitoring/__init__.py
  - src/tennisprediction/monitoring/reports.py
  - src/tennisprediction/monitoring/alerts.py
  - tests/unit/test_live_monitor_reports.py
autonomous: true
requirements:
  - OPS-01
  - OPS-02
must_haves:
  truths:
    - "Operator can review a polished terminal and persisted file report built from the existing Phase 06 monitoring outputs per D-02 and D-03."
    - "Every accepted opportunity report shows match, ticker, model probability, market probability, edge, expected value, liquidity, mapping confidence, and an advisory recommendation per D-03 through D-05."
    - "Rejected and excluded counts plus health warnings remain visible beside accepted opportunities so the report stays evidence-forward per D-03."
    - "Alert delivery remains terminal/file-only with no external notification channel or parallel report storage path per D-01 and D-02."
  artifacts:
    - path: "src/tennisprediction/monitoring/alerts.py"
      provides: "Operator-facing advisory report assembly and persisted terminal/file exports"
      exports: ["build_operator_report_rows", "render_operator_report"]
    - path: "src/tennisprediction/monitoring/reports.py"
      provides: "Canonical monitoring artifact writer extended with persisted operator-report outputs"
      exports: ["write_live_monitor_reports", "render_live_monitor_console"]
    - path: "tests/unit/test_live_monitor_reports.py"
      provides: "Regression coverage for ranked operator report fields, health warnings, and persisted file exports"
  key_links:
    - from: "src/tennisprediction/monitoring/reports.py"
      to: "src/tennisprediction/monitoring/alerts.py"
      via: "one ranked accepted/rejected row contract feeds both persisted artifacts and the human-facing report"
      pattern: "operator_report|ranked_rows|accepted_rows|rejected_rows"
    - from: "src/tennisprediction/monitoring/alerts.py"
      to: "reports/monitoring/<run_id>"
      via: "terminal/file-only export remains inside the existing Phase 06 report directory per D-02"
      pattern: "operator_report.txt|summary.json|accepted_opportunities"
---

<objective>
Create the opportunity-report slice for Phase 07 by upgrading the existing Phase 06 monitoring seam into a polished, advisory, operator-facing terminal/file report.

Purpose: Deliver OPS-01 and the report-facing portion of OPS-02 without violating D-01 through D-05.
Output: A shared ranked-row report surface, persisted operator-report files inside the existing monitoring directory, and failing-then-passing tests for the enriched advisory report contract.
</objective>

## Phase Goal

**As an** operator, **I want to** review a concise advisory report for each one-shot monitoring run, **so that** I can inspect actionable ATP-to-Kalshi opportunities and the health context behind them.

<execution_context>
@/Users/andrewlay/.codex/gsd-core/workflows/execute-plan.md
@/Users/andrewlay/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-alerts-and-operational-hardening/07-CONTEXT.md
@.planning/phases/07-alerts-and-operational-hardening/07-RESEARCH.md
@.planning/phases/07-alerts-and-operational-hardening/07-PATTERNS.md
@.planning/phases/07-alerts-and-operational-hardening/07-VALIDATION.md
@.planning/phases/06-market-mapping-executable-pricing-and-live-ev-monitor/06-04-SUMMARY.md
@src/tennisprediction/monitoring/reports.py
@src/tennisprediction/backtesting/reports.py
@tests/unit/test_live_monitor_reports.py
</context>

## Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Auditable persisted reports and operator-visible opportunity outputs | 07-01 | COVERED | This slice owns the human-facing report surface on top of Phase 06 artifacts. |
| REQ | OPS-01 | Terminal and persisted opportunity reports with required fields | 07-01 | COVERED | The advisory report is built from canonical accepted/rejected rows and persisted beside them. |
| REQ | OPS-02 | Configurable alert/report selection and channel settings | 07-01 | COVERED | This slice consumes the terminal/file-only settings added in 07-03 and keeps delivery scoped to D-01. |
| RESEARCH | R-07-01-A | Extend `src/tennisprediction/monitoring/reports.py` instead of creating a parallel report path | 07-01 | COVERED | The existing `reports/monitoring/<run_id>` seam remains canonical per D-02. |
| RESEARCH | R-07-01-B | Use Rich `Console(record=True)` style exports for terminal/file-first operator output | 07-01 | COVERED | One render path produces terminal output and persisted text artifacts. |
| CONTEXT | D-01 | Alert delivery remains terminal/file-only | 07-01 | COVERED | No external channels or webhook-style outputs appear in this slice. |
| CONTEXT | D-02 | Build on existing Phase 06 persisted monitoring outputs | 07-01 | COVERED | The advisory report is derived from the same ranked accepted/rejected row contract. |
| CONTEXT | D-03 | Focus on actionable accepted opportunities plus rejected/excluded counts and health warnings | 07-01 | COVERED | The report leads with accepted rows and preserves rejection/health context. |
| CONTEXT | D-04 | Rich layout details are delegated to the agent as long as output stays concise and evidence-forward | 07-01 | COVERED | Tests lock required content and advisory semantics, not a brittle string-for-string layout. |
| CONTEXT | D-05 | Recommendation language stays read-only and advisory | 07-01 | COVERED | Recommendation fields are advisory labels, not execution verbs. |

## Artifacts this phase produces

- Files: `src/tennisprediction/monitoring/__init__.py`, `src/tennisprediction/monitoring/reports.py`, `src/tennisprediction/monitoring/alerts.py`, `tests/unit/test_live_monitor_reports.py`
- Functions: `build_operator_report_rows`, `render_operator_report`, `write_live_monitor_reports`
- Output contract: `reports/monitoring/<run_id>/summary.json`, `accepted_opportunities.parquet`, `rejected_opportunities.parquet`, `ranked_opportunities.csv`, and `operator_report.txt` from the same canonical ranked rows

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for the enriched advisory report contract</name>
  <read_first>
.planning/phases/07-alerts-and-operational-hardening/07-CONTEXT.md
.planning/phases/07-alerts-and-operational-hardening/07-VALIDATION.md
src/tennisprediction/monitoring/reports.py
src/tennisprediction/backtesting/reports.py
tests/unit/test_live_monitor_reports.py
  </read_first>
  <files>tests/unit/test_live_monitor_reports.py</files>
  <behavior>
    - Test 1: `write_live_monitor_reports()` still persists canonical accepted/rejected monitoring artifacts inside `reports/monitoring/<run_id>` per D-02 while also producing a persisted operator report file from the same ranked rows.
    - Test 2: accepted report rows expose the required OPS-01 fields: match identity, ticker, model probability, market probability, edge, expected value, liquidity, mapping confidence, and recommendation.
    - Test 3: the operator-facing summary includes rejected/excluded counts and health-warning banners for stale, ambiguous, unmatched, or thin-liquidity conditions per D-03.
    - Test 4: recommendation labels are advisory-only per D-05 and do not imply order placement or execution.
  </behavior>
  <action>Expand `tests/unit/test_live_monitor_reports.py` so it fails against the current Phase 06 implementation. Lock the persisted `operator_report.txt` export, the required OPS-01 report fields, and the presence of rejection-count plus health-warning context. Keep the tests coupled to canonical ranked rows and `reports/monitoring/<run_id>` so D-02 is enforced directly rather than inferred later.</action>
  <verify>
    <automated>! UV_CACHE_DIR=.uv-cache python3 -m uv run pytest -q tests/unit/test_live_monitor_reports.py -x</automated>
  </verify>
  <acceptance_criteria>
    - The failing tests prove the current Phase 06 report surface is insufficient for OPS-01.
    - The test contract preserves the Phase 06 canonical output directory and accepted/rejected row truth source.
    - Advisory-only semantics are encoded in tests without over-constraining the Rich layout details delegated by D-04.
  </acceptance_criteria>
  <done>The slice has failing tests that lock the enriched advisory report contract before implementation starts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the advisory terminal/file report on top of the Phase 06 seam</name>
  <read_first>
tests/unit/test_live_monitor_reports.py
src/tennisprediction/monitoring/reports.py
src/tennisprediction/backtesting/reports.py
.planning/phases/07-alerts-and-operational-hardening/07-PATTERNS.md
  </read_first>
  <files>src/tennisprediction/monitoring/__init__.py, src/tennisprediction/monitoring/reports.py, src/tennisprediction/monitoring/alerts.py</files>
  <action>Create `src/tennisprediction/monitoring/alerts.py` as the operator-facing presentation seam and extend `src/tennisprediction/monitoring/reports.py` so one ranked accepted/rejected row contract drives both persisted machine-readable outputs and the advisory terminal/file report per D-02 and D-03. Keep delivery terminal/file-only per D-01, persist `operator_report.txt` under the existing `reports/monitoring/<run_id>` directory, and expose advisory recommendation labels that remain explicitly read-only per D-05. Reuse the Phase 06 ranking logic, keep rejected/excluded counts plus health warnings visible, and avoid inventing any `reports/alerts/` or second schema path.</action>
  <verify>
    <automated>UV_CACHE_DIR=.uv-cache python3 -m uv run pytest -q tests/unit/test_live_monitor_reports.py -x</automated>
  </verify>
  <acceptance_criteria>
    - The enriched report continues to persist the Phase 06 accepted/rejected artifacts and adds a persisted operator report file without changing the canonical row contract.
    - The terminal/file report is concise, evidence-forward, and focuses on accepted opportunities while still surfacing rejected/excluded counts and health warnings.
    - Recommendation language is advisory-only and cannot be confused with automated execution behavior.
  </acceptance_criteria>
  <done>Phase 07 now has a polished terminal/file advisory report built directly on the existing Phase 06 monitoring outputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accepted/rejected monitoring rows→operator report | The human-facing report must not fork from the canonical machine-readable output contract |
| advisory recommendation→operator action | Recommendation text must stay read-only and never imply execution |
| report persistence→repo-local filesystem | Persisted report files must stay inside the existing repo-local monitoring output tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | `src/tennisprediction/monitoring/reports.py` | mitigate | Extend the existing `reports/monitoring/<run_id>` seam per D-02 instead of introducing a second report storage path or schema. |
| T-07-01-02 | Repudiation | `src/tennisprediction/monitoring/alerts.py` | mitigate | Keep rejected/excluded counts, health warnings, and canonical accepted/rejected artifacts visible beside the advisory report so later reviews can reconstruct why a run looked healthy or degraded. |
| T-07-01-03 | Elevation of Privilege | persisted recommendation language | mitigate | Use explicit advisory-only recommendation labels and forbid execution-oriented wording per D-05. |
</threat_model>

<verification>
- `UV_CACHE_DIR=.uv-cache python3 -m uv run pytest -q tests/unit/test_live_monitor_reports.py -x`
- `UV_CACHE_DIR=.uv-cache python3 -m uv run ruff check src/tennisprediction/monitoring tests/unit/test_live_monitor_reports.py`
</verification>

<success_criteria>
- Operator can review a polished terminal/file report for a monitoring run without leaving the existing Phase 06 report seam.
- Required OPS-01 fields, rejected/excluded counts, and health warnings are all visible from one advisory surface.
- The report remains explicitly read-only and terminal/file-only.
</success_criteria>

<output>
Create `.planning/phases/07-alerts-and-operational-hardening/07-01-SUMMARY.md` when done
</output>
