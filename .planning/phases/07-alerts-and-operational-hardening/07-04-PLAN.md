---
phase: 07-alerts-and-operational-hardening
plan: 04
type: execute
wave: 4
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - .pre-commit-config.yaml
  - .github/workflows/ci.yml
  - docs/operations.md
autonomous: true
requirements:
  - OPS-02
  - OPS-05
  - OPS-06
must_haves:
  truths:
    - "Developer can run one documented local quality-gate surface that checks linting, formatting, typing, and critical leakage/EV/CLI/report/scan-orchestration logic."
    - "Repo CI runs the same quality gates as local development instead of a different hidden checklist."
    - "Operator documentation explains setup, Kalshi configuration, one-shot CLI commands, output files, trust limits, v1 scope boundaries, and the shipped one-shot settings surface aligned with D-06 and D-07."
  artifacts:
    - path: ".pre-commit-config.yaml"
      provides: "Repo-local pre-commit hooks for lint, format, typing, and critical tests"
    - path: ".github/workflows/ci.yml"
      provides: "Repository CI workflow running the same uv-based quality gates"
    - path: "docs/operations.md"
      provides: "Operator runbook and trust-boundary documentation for the Phase 07 workflow"
  key_links:
    - from: ".pre-commit-config.yaml"
      to: ".github/workflows/ci.yml"
      via: "both surfaces execute the same uv-based lint, format, typing, and targeted test commands"
      pattern: "ruff check|ruff format --check|mypy src|pytest"
    - from: "docs/operations.md"
      to: "src/tennisprediction/cli.py"
      via: "the runbook documents the shipped one-shot command tree and output locations"
      pattern: "ingest|build-features|train|evaluate|backtest|scan|report"
    - from: "docs/operations.md"
      to: ".planning/phases/07-alerts-and-operational-hardening/07-CONTEXT.md"
      via: "documentation preserves the read-only, terminal/file-only, one-shot scope fences"
      pattern: "terminal/file-only|read-only|one-shot|polling"
---

<objective>
Create the quality-gate and documentation closeout slice for Phase 07 by packaging repo-local and CI checks, then documenting how to run and trust the final one-shot workflow.

Purpose: Deliver OPS-05 and OPS-06, and document the final one-shot OPS-02 settings surface without violating D-06 or D-07.
Output: Pre-commit and GitHub Actions quality gates plus an operator runbook that explains setup, commands, outputs, and trust boundaries.
</objective>

## Phase Goal

**As a** developer or operator, **I want to** run repeatable quality gates and read one accurate runbook for the final workflow, **so that** I can operate the ATP-to-Kalshi system with clear trust limits and reproducible checks.

<execution_context>
@/Users/andrewlay/.codex/gsd-core/workflows/execute-plan.md
@/Users/andrewlay/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-alerts-and-operational-hardening/07-CONTEXT.md
@.planning/phases/07-alerts-and-operational-hardening/07-RESEARCH.md
@.planning/phases/07-alerts-and-operational-hardening/07-VALIDATION.md
@docs/data-contracts.md
@pyproject.toml
@src/tennisprediction/cli.py
@src/tennisprediction/logging.py
</context>

## Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Quality-gated, documented, operator-ready workflow | 07-04 | COVERED | This closeout slice packages gates and documentation on top of the shipped command/report flow. |
| REQ | OPS-02 | One-shot settings surface covers thresholds, model artifact selection, report selection, storage paths, and terminal/file alert channel settings | 07-04 | COVERED | The runbook documents the shipped configuration surface and keeps continuous-run controls out of scope under D-06 and D-07. |
| REQ | OPS-05 | CI or local quality gates for tests, linting, formatting, typing, and critical leakage/EV logic | 07-04 | COVERED | Pre-commit and GitHub Actions run the same uv-based checks. |
| REQ | OPS-06 | Setup and operations documentation for commands, outputs, limitations, and v1 boundaries | 07-04 | COVERED | `docs/operations.md` is the operator runbook/checklist artifact. |
| RESEARCH | R-07-04-A | Use `pre-commit` plus GitHub Actions wired through uv-native commands rather than ad hoc shell-only gates | 07-04 | COVERED | Quality gates are runnable locally and in CI from one documented surface. |
| RESEARCH | R-07-04-B | Docs must explain one-shot workflow, output directories, and trust limits | 07-04 | COVERED | The runbook focuses on execution flow and signal trust boundaries. |
| CONTEXT | D-06 | One-shot commands only | 07-04 | COVERED | Docs and quality gates do not advertise or test daemonized behavior. |
| CONTEXT | D-07 | Thresholds, artifact/report selection, and storage paths only; polling controls are out of scope | 07-04 | COVERED | The runbook documents the shipped one-shot settings surface and keeps continuous-run controls explicitly out of scope. |
| CONTEXT | D-08 | Strong local quality gates and repo-level CI expectations | 07-04 | COVERED | This slice creates both `.pre-commit-config.yaml` and `.github/workflows/ci.yml`. |
| CONTEXT | D-09 | Documentation must be good enough for setup, execution, outputs, and trust limits | 07-04 | COVERED | The runbook doubles as the operator checklist artifact from `07-VALIDATION.md`. |

## Artifacts this phase produces

- Files: `.pre-commit-config.yaml`, `.github/workflows/ci.yml`, `docs/operations.md`
- Quality-gate contract: uv-based lint, format, typing, CLI/report/logging/scan-orchestration tests, and critical leakage/EV regression checks
- Documentation contract: setup, Kalshi configuration, one-shot commands, output directories, backtest limitations, and v1 scope boundaries

<tasks>

<task type="auto">
  <name>Task 1: Package the local and CI quality gates around the Phase 07 validation contract</name>
  <read_first>
.planning/phases/07-alerts-and-operational-hardening/07-VALIDATION.md
.planning/phases/07-alerts-and-operational-hardening/07-RESEARCH.md
pyproject.toml
  </read_first>
  <files>.pre-commit-config.yaml, .github/workflows/ci.yml</files>
  <action>Create `.pre-commit-config.yaml` and `.github/workflows/ci.yml` that run the same uv-based quality checks for this repo: `ruff check .`, `ruff format --check .`, `mypy src`, and the critical test surface from `07-VALIDATION.md`, including CLI, report, logging, scan-orchestration, leakage, and EV regressions. Keep the workflow repo-local and one-shot, use the locked Python 3.12/uv environment, and avoid watch-mode, shell-only, or undocumented quality shortcuts. The CI workflow should sync the lockfile with the groups needed by the shipped test surface, then execute the same commands documented for local development.</action>
  <verify>
    <automated>UV_CACHE_DIR=.uv-cache python3 -m uv run pre-commit run --all-files</automated>
  </verify>
  <acceptance_criteria>
    - Local hooks and CI run the same lint, format, typing, and critical regression commands.
    - The quality-gate surface covers leakage, EV, CLI, logging, reporting, and scan-orchestration regressions instead of only generic style checks.
    - The added config closes the Wave 0 `.pre-commit-config.yaml` and `.github/workflows/ci.yml` gaps from `07-VALIDATION.md`.
  </acceptance_criteria>
  <done>The repo has executable local and CI quality-gate configs aligned with the Phase 07 validation contract.</done>
</task>

<task type="auto">
  <name>Task 2: Write the operator runbook and trust-boundary checklist for the final one-shot workflow</name>
  <read_first>
.planning/phases/07-alerts-and-operational-hardening/07-CONTEXT.md
.planning/phases/07-alerts-and-operational-hardening/07-RESEARCH.md
.planning/phases/07-alerts-and-operational-hardening/07-VALIDATION.md
docs/data-contracts.md
src/tennisprediction/cli.py
  </read_first>
  <files>docs/operations.md</files>
  <action>Create `docs/operations.md` as the operator runbook and checklist artifact for OPS-06. Document setup with the repo-standard uv workflow, the Jeff Sackmann and Kalshi inputs, the shipped one-shot CLI commands from ingestion through reporting, expected output directories and filenames, audit-log locations, backtest and provenance limitations, and the ATP-only, Kalshi-only, read-only scope boundaries. Document the shipped OPS-02 settings surface for one-shot runs: thresholds, model artifact selection, report selection, storage paths, and terminal/file alert channels, while making clear that continuous polling controls remain out of scope under D-06 and D-07. Keep the doc focused on how to run the system and how to distrust or interpret signals, not on future enhancements.</action>
  <verify>
    <automated>rg -q "one-shot|read-only|reports/monitoring|polling.*deferred|Kalshi|ATP-only" docs/operations.md</automated>
    <human-check>Read `docs/operations.md` and confirm it explains setup, command flow, outputs, trust limits, and the one-shot settings scope clearly enough for a developer/operator to use the workflow without extra tribal knowledge.</human-check>
  </verify>
  <acceptance_criteria>
    - The runbook covers setup, command execution, outputs, and trust boundaries with enough detail for a new operator to use the system.
    - The documentation explicitly preserves the locked one-shot, terminal/file-only, read-only scope.
    - The doc doubles as the runbook/checklist artifact required by `07-VALIDATION.md`.
  </acceptance_criteria>
  <done>Phase 07 has a final operator runbook that matches the shipped CLI/report/logging surfaces and explains the workflow's scope and trust limits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer workstation→quality-gate config | Local hooks and CI must execute the same checks rather than divergent hidden rules |
| shipped CLI/report behavior→documentation | Docs must match the real operator path and not advertise unimplemented continuous behavior |
| repo quality gate→critical domain logic | Leakage and EV regressions must stay inside the mandatory gate surface, not just style tooling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Repudiation | `.pre-commit-config.yaml` and `.github/workflows/ci.yml` | mitigate | Run the same uv-based lint, format, typing, and critical domain test commands locally and in CI so gate results are comparable. |
| T-07-04-02 | Tampering | `docs/operations.md` | mitigate | Document only the shipped one-shot CLI/report/logging surfaces and the final one-shot settings contract, while keeping continuous polling controls explicitly out of scope. |
| T-07-04-03 | Denial of Service | missing critical regression gates | mitigate | Keep leakage, EV, CLI, logging, and reporting regressions in the mandatory gate surface rather than relying on ad hoc manual checks. |
</threat_model>

<verification>
- `UV_CACHE_DIR=.uv-cache python3 -m uv run pre-commit run --all-files`
- `UV_CACHE_DIR=.uv-cache python3 -m uv run ruff check . && UV_CACHE_DIR=.uv-cache python3 -m uv run ruff format --check . && UV_CACHE_DIR=.uv-cache python3 -m uv run mypy src && UV_CACHE_DIR=.uv-cache python3 -m uv run pytest -q tests/unit/test_cli_smoke.py tests/unit/test_cli_commands.py tests/unit/test_operational_logging.py tests/unit/test_live_monitor_reports.py tests/unit/test_live_scan_orchestration.py tests/unit/test_feature_leakage.py tests/unit/test_backtesting_decisions.py tests/unit/test_live_scan_pricing_contract.py -x`
</verification>

<success_criteria>
- Developers have one executable local and CI quality-gate surface for Phase 07.
- The operator runbook matches the shipped CLI, report, and logging behavior and explains trust limits clearly.
- The documentation preserves the resolved one-shot settings contract and keeps continuous-run controls out of scope.
</success_criteria>

<output>
Create `.planning/phases/07-alerts-and-operational-hardening/07-04-SUMMARY.md` when done
</output>
