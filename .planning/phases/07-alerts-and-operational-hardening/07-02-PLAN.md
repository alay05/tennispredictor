---
phase: 07-alerts-and-operational-hardening
plan: 02
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-03
files_modified:
  - src/tennisprediction/logging.py
  - src/tennisprediction/operations.py
  - src/tennisprediction/kalshi/jobs.py
  - src/tennisprediction/market_mapping/resolver.py
  - src/tennisprediction/ev/opportunity.py
  - src/tennisprediction/monitoring/scan.py
  - src/tennisprediction/monitoring/alerts.py
  - tests/unit/test_operational_logging.py
autonomous: true
requirements:
  - OPS-03
must_haves:
  truths:
    - "Each one-shot operator run writes correlated audit logs with stable run, command, and stage context instead of free-form-only strings."
    - "Audit logging covers ingestion, feature generation, training, evaluation, backtesting, Kalshi collection, mapping, EV filtering, and advisory report decisions."
    - "Sensitive Kalshi credentials and private-key details are redacted or omitted from logs while repo-local file persistence remains audit-friendly."
  artifacts:
    - path: "src/tennisprediction/logging.py"
      provides: "Context-aware console/file logging bootstrap with redaction and stable operational fields"
      exports: ["configure_logging", "bind_audit_context"]
    - path: "src/tennisprediction/operations.py"
      provides: "One-shot pipeline wrappers instrumented with start/finish audit events"
      exports: ["ingest_snapshot", "build_features", "train_artifact_bundle", "run_backtest", "report_monitoring_run"]
    - path: "tests/unit/test_operational_logging.py"
      provides: "Regression coverage for stable logging fields, handler fan-out, and secret redaction"
  key_links:
    - from: "src/tennisprediction/operations.py"
      to: "src/tennisprediction/logging.py"
      via: "per-command logger adapters stamp run, command, and stage context on every one-shot workflow"
      pattern: "run_id|command|stage|artifact_run_id"
    - from: "src/tennisprediction/monitoring/scan.py"
      to: "src/tennisprediction/market_mapping/resolver.py"
      via: "mapping-state and rejection summaries are logged with the same run context"
      pattern: "mapping_state|mapping_confidence|rejection_reason"
    - from: "src/tennisprediction/monitoring/alerts.py"
      to: "src/tennisprediction/logging.py"
      via: "advisory report decisions are logged without leaking secrets or execution semantics"
      pattern: "recommendation|operator_report|health_warning"
---

<objective>
Create the operational audit-logging slice for Phase 07 by turning the current bootstrap-only logger into a correlated, repo-local audit trail across pipeline and monitoring decisions.

Purpose: Deliver OPS-03 with stable run context, redaction, and cross-module decision visibility while preserving the one-shot read-only boundary.
Output: Context-aware logging helpers, instrumented one-shot pipeline wrappers, targeted logging in market/mapping/EV/report seams, and failing-then-passing logging tests.
</objective>

## Phase Goal

**As an** operator, **I want to** trace each one-shot workflow run across pipeline stages and monitoring decisions, **so that** I can audit how an opportunity report was produced and why markets were accepted or rejected.

<execution_context>
@/Users/andrewlay/.codex/gsd-core/workflows/execute-plan.md
@/Users/andrewlay/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-alerts-and-operational-hardening/07-CONTEXT.md
@.planning/phases/07-alerts-and-operational-hardening/07-RESEARCH.md
@.planning/phases/07-alerts-and-operational-hardening/07-PATTERNS.md
@.planning/phases/07-alerts-and-operational-hardening/07-VALIDATION.md
@src/tennisprediction/logging.py
@src/tennisprediction/cli.py
@src/tennisprediction/kalshi/jobs.py
@src/tennisprediction/market_mapping/resolver.py
@src/tennisprediction/ev/opportunity.py
@src/tennisprediction/monitoring/scan.py
</context>

## Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Audited CLI workflow and report generation | 07-02 | COVERED | This slice adds the correlated audit trail behind the operator workflow. |
| REQ | OPS-03 | Audit logging across pipeline, market, mapping, EV, and alert decisions | 07-02 | COVERED | Logging spans one-shot command wrappers and the downstream monitoring decision seams. |
| RESEARCH | R-07-02-A | Replace bootstrap-only logging with contextual file/console handlers using stdlib logging adapters or filters | 07-02 | COVERED | `configure_logging()` becomes the single context-aware bootstrap seam. |
| RESEARCH | R-07-02-B | Avoid secret leakage while adding richer audit fields | 07-02 | COVERED | Redaction is a first-class test and implementation concern. |
| CONTEXT | D-05 | Advisory surfaces must remain read-only and not imply execution | 07-02 | COVERED | Alert-decision logs stay advisory and never log or imply execution actions. |
| CONTEXT | D-06 | Phase 07 remains one-shot only | 07-02 | COVERED | Audit context is stamped per command/run rather than via daemon or scheduler state. |

## Artifacts this phase produces

- Files: `src/tennisprediction/logging.py`, `src/tennisprediction/operations.py`, `src/tennisprediction/kalshi/jobs.py`, `src/tennisprediction/market_mapping/resolver.py`, `src/tennisprediction/ev/opportunity.py`, `src/tennisprediction/monitoring/scan.py`, `src/tennisprediction/monitoring/alerts.py`, `tests/unit/test_operational_logging.py`
- Logging fields: `run_id`, `command`, `stage`, `artifact_run_id`, `market_ticker`, `mapping_state`, `decision_state`
- Output contract: repo-local console and file audit logs with redaction for Kalshi secrets and key-material paths

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing operational logging tests for context, fan-out, and redaction</name>
  <read_first>
.planning/phases/07-alerts-and-operational-hardening/07-CONTEXT.md
.planning/phases/07-alerts-and-operational-hardening/07-VALIDATION.md
src/tennisprediction/logging.py
src/tennisprediction/monitoring/scan.py
src/tennisprediction/ev/opportunity.py
  </read_first>
  <files>tests/unit/test_operational_logging.py</files>
  <behavior>
    - Test 1: `configure_logging()` creates correlated console/file handler output with stable fields such as `run_id`, `command`, and `stage`.
    - Test 2: command wrappers emit start/finish events for ingestion, feature generation, training, evaluation, backtesting, snapshot collection, scanning, and reporting.
    - Test 3: mapping, EV, and alert seams log reason-coded counts or summaries using the same run context.
    - Test 4: secrets such as Kalshi access keys, auth headers, and private-key contents or raw paths are redacted or omitted from logs.
  </behavior>
  <action>Create the missing Wave 0 file `tests/unit/test_operational_logging.py` and make it fail against the current bootstrap-only logging implementation. Use temporary repo-local log paths, `caplog`, and monkeypatched wrappers so the test contract locks handler fan-out, stable context fields, and secret redaction without depending on an external Kalshi service.</action>
  <verify>
    <automated>! UV_CACHE_DIR=.uv-cache python3 -m uv run pytest -q tests/unit/test_operational_logging.py -x</automated>
  </verify>
  <acceptance_criteria>
    - The new logging test fails before implementation and closes the Wave 0 gap called out by `07-VALIDATION.md`.
    - Stable operational fields are asserted directly, not inferred from arbitrary message strings.
    - Redaction behavior is part of the locked test contract.
  </acceptance_criteria>
  <done>The logging slice has failing tests that lock contextual audit fields and redaction behavior before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement correlated audit logging across one-shot pipeline and monitoring seams</name>
  <read_first>
tests/unit/test_operational_logging.py
src/tennisprediction/logging.py
src/tennisprediction/operations.py
src/tennisprediction/kalshi/jobs.py
src/tennisprediction/market_mapping/resolver.py
src/tennisprediction/ev/opportunity.py
src/tennisprediction/monitoring/scan.py
src/tennisprediction/monitoring/alerts.py
.planning/phases/07-alerts-and-operational-hardening/07-PATTERNS.md
  </read_first>
  <files>src/tennisprediction/logging.py, src/tennisprediction/operations.py, src/tennisprediction/kalshi/jobs.py, src/tennisprediction/market_mapping/resolver.py, src/tennisprediction/ev/opportunity.py, src/tennisprediction/monitoring/scan.py, src/tennisprediction/monitoring/alerts.py</files>
  <action>Replace `basicConfig()` in `src/tennisprediction/logging.py` with one project-owned bootstrap that emits both console and repo-local file logs, adds explicit redaction, and exposes a helper such as `bind_audit_context()` for stable extra fields. Instrument `src/tennisprediction/operations.py` so each one-shot command wrapper logs start, finish, input/output artifact paths, and run identifiers for ingestion, features, training, evaluation, backtesting, snapshot collection, live scan, and reporting. Add decision-level summaries in `src/tennisprediction/kalshi/jobs.py`, `src/tennisprediction/market_mapping/resolver.py`, `src/tennisprediction/ev/opportunity.py`, `src/tennisprediction/monitoring/scan.py`, and `src/tennisprediction/monitoring/alerts.py`, always using stable fields rather than free-form-only messages and never logging Kalshi secrets or key material.</action>
  <verify>
    <automated>UV_CACHE_DIR=.uv-cache python3 -m uv run pytest -q tests/unit/test_operational_logging.py tests/unit/test_live_monitor_reports.py tests/unit/test_cli_commands.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Audit logs correlate each one-shot command and downstream monitoring decision with stable run context.
    - Logging covers the full Phase 07 operator workflow without leaking secrets.
    - The implementation reuses one project bootstrap seam instead of ad hoc log wiring in each command.
  </acceptance_criteria>
  <done>Phase 07 now emits correlated, repo-local audit logs across the pipeline, market, mapping, EV, and advisory report layers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI or wrapper inputs→logging context | Untrusted runtime inputs must become stable, redacted logging fields |
| Kalshi credentials→audit trail | Secrets must never cross into console or file logs |
| mapping/EV/alert decisions→operator audit review | Decision counts and reasons must be logged consistently enough for repudiation-resistant review |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Information Disclosure | `src/tennisprediction/logging.py` and downstream call sites | mitigate | Add explicit redaction and omit raw Kalshi secrets, auth headers, and private-key contents or paths from logged context. |
| T-07-02-02 | Repudiation | `src/tennisprediction/operations.py` and monitoring seams | mitigate | Stamp stable `run_id`, `command`, `stage`, and decision-summary fields on every one-shot workflow so later audits can reconstruct what happened. |
| T-07-02-03 | Tampering | free-form-only logging in downstream modules | mitigate | Route all structured audit context through one bootstrap/helper seam and assert it with targeted tests in `tests/unit/test_operational_logging.py`. |
</threat_model>

<verification>
- `UV_CACHE_DIR=.uv-cache python3 -m uv run pytest -q tests/unit/test_operational_logging.py -x`
- `UV_CACHE_DIR=.uv-cache python3 -m uv run pytest -q tests/unit/test_operational_logging.py tests/unit/test_live_monitor_reports.py tests/unit/test_cli_commands.py -x`
- `UV_CACHE_DIR=.uv-cache python3 -m uv run ruff check src/tennisprediction/logging.py src/tennisprediction/operations.py src/tennisprediction/monitoring src/tennisprediction/kalshi/jobs.py src/tennisprediction/market_mapping/resolver.py src/tennisprediction/ev/opportunity.py tests/unit/test_operational_logging.py`
</verification>

<success_criteria>
- Operators can correlate every one-shot run from command entry through mapping, EV, and advisory report decisions.
- Stable audit fields exist in console and file logs without leaking Kalshi secrets.
- The logging surface is centralized enough that later phases can extend it without duplicating redaction or context logic.
</success_criteria>

<output>
Create `.planning/phases/07-alerts-and-operational-hardening/07-02-SUMMARY.md` when done
</output>
